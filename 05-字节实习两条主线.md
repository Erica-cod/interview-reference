# 字节实习两条主线：ReactLynx 与 Markdown Repair

## 主线一：ReactLynx Harmony 稳定性治理

### 先把“数据回设”说清楚

> 数据回设不是“接口数据回来得慢”，而是同一段 UI 在两个执行阶段拿到了不一致的输入：Harmony 首屏由主线程先用默认值、空值或临时值直出，后台 React 线程算出真实值后，再通过 `UpdateComponentData` patch 回原生侧。用户看到的就是首帧在 A 状态，下一帧被改成 B 状态，表现为闪白、内容后补或布局跳变。

面试中固定使用“数据回设”，不要混说成“数据回撤、回收、回传”。`__temp` 只是一条线索，必须和运行时风险提示、两次更新的时间顺序、视觉现象一起构成证据，不能说“看到 `__temp` 就一定有 bug”。

### 30 秒结论版

> 这个需求本质不是普通样式兼容，而是 ReactLynx 双线程首屏模型下的稳定性治理。为了首帧直出，主线程会先同步渲染一版 UI，后台线程再运行完整 React 并 hydrate/patch。如果 render 依赖主线程首屏拿不到的运行时值，编译器会抽成 `__temp`，后台算完后通过 `UpdateComponentData` 回写，表现为闪白、内容后补或布局跳变。
>
> 我通过 trace 和日志排除了接口与资源加载问题，再把风险归为顶层运行时值早读、组件隐式依赖两类。方案上分别采用使用点懒读取、父层整理后 props 显式传入，只有真正会随生命周期变化并驱动 UI 的数据才 state 化。改后再用相同入口冷启动，对比 trace 中的首轮输入和后续 patch、真机录屏及共享组件回归，确认我负责链路里的跳变不再复现。

### 2 分钟字段级回答：按这个顺序讲

> 背景上，高考落地页在 Harmony 冷启动首屏会出现闪白、内容后补或布局跳变。我的任务不是泛泛优化性能，而是定位哪些组件的首屏输入在主线程直出和后台 React 回写之间不一致，并治理我负责的风险点。
>
> 我先固定同一个系统版本、页面入口和冷启动条件，保留问题录屏与 trace。以 PopToast 为例，复盘示例里参与布局的字段是 `bottom/visible`：首帧相关判断被编译为 `__temp`，主线程先按默认 bottom 或空样式计算；后台线程完成运行时值读取后，又通过 `UpdateComponentData` 把真实值 patch 回去，所以视觉上会从默认位置跳到正确位置。同时接口日志显示数据按时返回，图片和样式加载也没有异常，因此我把网络慢、资源慢从主因里排除了。
>
> 根因有两类。第一类是在模块顶层读取 `globalProps/env/settings` 一类运行时值，初始化过早会把当时的值固化成旧快照；第二类是共享组件在 render 内部隐式读取 runtime/global/实例方法，主线程无法保证首帧同步得到同样的输入。
>
> 对第一类，我把顶层缓存改为真正使用时再读取，避免模块初始化阶段锁住旧值；对第二类，让父容器先整理首屏必须稳定的数据，再通过 props 显式传入。真正会在用户交互或生命周期中变化、而且变化后需要驱动 UI 的值才放 state；能从 props/state 同步算出的展示值保持局部派生。默认值只做不崩溃兜底，因为它不能阻止真实值到达后的二次回写。
>
> 验证上我做三层对比：第一，改造前后看 trace，确认对应字段不再出现“首轮默认/空值、随后真实值回写”的风险链路；第二，在同一设备和入口反复冷启动录屏，看首帧位置与稳定态是否一致；第三，回归 PopToast 的其他使用入口和交互，结合 QA 与灰度期风险提示确认没有新增问题。我没有单独负责全局大盘，所以不会编一个线上提升数字；我能确认的是自己负责组件的 trace 告警和可复现跳变消失。

### props 与 state 追问：用一条字段链说

复盘示例使用 `TabPage.tabList/activeTabKey`，适合练习回答结构：

> 改造前，TabPage 会在内部兜底读取 runtime cache。trace 中第一次 `UpdateComponentData` 时 `tabList` 还是空数组或 `undefined`，`activeTabKey` 也未确定；数据补齐后第二次更新才回设真实值，因此 Tab 区域会先空、再撑开。改造后，父容器根据 `pageType/query/defaultConfig` 先整理出稳定的 `tabList` 和 `initialActiveTabKey`，首轮就通过 props 传入，TabPage 不再在 render 内补读缓存。`tabList`、页面类型这类首屏结构数据属于父层输入；用户点击后变化并驱动局部 UI 的 `currentActiveKey` 才是组件 state。验证时看首轮 props 是否完整、空值到真实值的二次更新是否消失，而不是只说“数据流更清晰”。

**真实性红线：** `PopToast.bottom/visible`、`TabPage.tabList/activeTabKey`、`__temp_tabInfo` 都来自模拟复盘的示例回答。背诵前必须对照真实 MR/代码/trace 替换；如果并非真实字段，就保留回答结构，不要把字段说成亲手改过。

### 面试官连续追问时的证据链

| 追问 | 必须回答到的证据 |
| --- | --- |
| 你怎么知道不是接口慢？ | 接口日志/返回时机正常，图片与样式无异常；trace 显示首轮值与后续 patch 不一致 |
| trace 哪一段说明有风险？ | 具体组件 + 具体字段 + 首轮 `__temp`/默认值 + 后续 `UpdateComponentData` 真实值 |
| 为什么懒读取有用？ | 避免模块初始化时把动态 runtime 值冻结成旧快照；它不解决所有异步数据 |
| 为什么 props 化？ | 首屏结构数据由父层一次整理，子组件不再在 render 内隐式读 runtime/cache |
| 为什么不都放 state？ | 只把会变化且需要驱动 UI 的数据 state 化，避免双数据源和额外首屏更新 |
| 怎么证明改好了？ | 相同条件下 trace 前后对比 + 真机冷启动录屏 + 共享入口/QA/灰度回归 |
| 有线上提升数字吗？ | 没有就明确说组件级证据和负责范围，不把平台大盘说成个人指标 |

### 高频追问

**为什么不只补默认值？**

默认值只能避免空值异常，真实值到达后仍会回写；根因是数据边界和读取时机不稳定。

**为什么懒读取有效？**

它避免模块初始化时把动态运行时值锁成旧快照；但如果首屏时真实值本来就没准备好，还需 state 更新或平台兜底。

**为什么 props 化？**

把共享组件内部偷偷读全局，改成父层整理输入后显式传入。组件更接近纯渲染，排查也能从 props 入口定位。

**为什么不都放 state？**

state 只放接口返回、会影响展示且有更新语义的数据。纯派生值放 state 会制造第二份数据源。

## 主线二：Markdown 渲染质量治理

### 90 秒版本

> QA 和自动巡检持续回捞标题、加粗、列表和表格渲染 badcase。Markdown parser 对识别失败的内容通常不会报错，而是退化为 Text，所以不能依赖 parse error。我们也没有用正则扫全量原文，因为代码块、Latex、HTML 和命令行中都可能出现 `#`、`|`、`**`，误伤风险高。
>
> 我的方案是先解析 AST，只遍历疑似失败的 Text 节点，并根据父级上下文排除 Code、HTML、Latex、Table 等节点；正则只负责局部模式，AST 提供上下文。修复分两类：标记完整但 parser 边界过严的问题，例如中文标点包裹的加粗，走前置 Repair；原文确定缺少结构字符的问题，例如标题/有序列表缺空格、表格前缺空行，做最小后置 Repair。
>
> 测评不只看 Success，还单独统计 Fail 和 Worse，因为修复失败只是没收益，修坏原文才是负收益。最终对照组异常率 7.64%，前置加后置的 v2 为 3.23%，绝对下降 4.41 个百分点，相对下降 57.72%。

### 高频追问

- **为什么不用纯正则？** 正则没有结构上下文；AST 先限定 Text 节点和父级边界。
- **前置/后置怎么选？** parser 过严改解析边界；原文缺确定字符做最小文本修复。
- **如何防 Worse？** 上下文过滤、最小编辑、规则级灰度、保留原文、单独统计 Worse。
- **异步二次渲染闪烁怎么办？** 只在稳定片段或消息完成时触发，debounce，结果带 messageId/version，低置信不替换。
- **流式中间态呢？** rawContent 不变；renderContent 可临时补全未闭合代码块/链接，下一帧从最新 rawContent 重新计算。

## Garfish 与 AI 巡检：只备一句

**Garfish：** 微前端难点不是加载，而是通信、路由、样式、公共依赖和卸载清理等边界治理；我的工作集中在业务模块接入和问题协作，不把平台架构说成个人设计。

**AI 巡检：** 它补充传统 E2E 对视觉异常和语义问题覆盖不足；模型输出必须用结构化结果、规则二次判断和人工复核约束。若被问 AI Native，再展开“模型能力 + 确定性工程兜底”。

## ReactLynx 治理方案决策表

| 方案 | 解决的问题 | 代价/边界 | 本次定位 |
| --- | --- | --- | --- |
| 默认值/null 兜底 | 防止空值导致崩溃或非法样式 | 真实值到达后仍可能回设 | 只做最后兜底 |
| 运行时懒读取 | 避免模块初始化过早缓存旧值 | 首屏真实值未准备好时无能为力 | 处理 globalProps/env 早读 |
| 父层读取 + props 化 | 显式共享组件的运行时输入 | 父层 props 增多，需要边界设计 | 处理隐式全局依赖 |
| state 化 | 让有生命周期的数据可追踪并驱动 UI | 可能制造第二份数据源 | 只用于接口返回等真实状态 |
| 局部派生计算 | 减少实例字段/方法与重复状态 | 复杂重计算仍需性能优化 | 处理纯展示值 |
| Lepus/主线程化 | 主线程同步计算，最彻底减少回设 | 只适合纯计算，运行时能力受限 | 有明确收益时局部使用 |
| Harmony 平台分支 | 快速隔离 viewpager/list 等平台风险 | 产生分支维护和测试矩阵 | 原平台稳定时采用 |
| 全量架构重构 | 长期统一模型和代码结构 | 改动与回归风险高 | 高流量短周期项目不采用 |

### 为什么最终是组合方案

> 两类根因不同，所以不能用一个手段包打天下。早读旧值用懒读取；共享组件偷偷读全局用 props 化；接口返回并影响展示的数据用 state；纯计算保持局部派生；原生组件差异用 Harmony 平台隔离。默认值只保证最坏情况下不崩，不承担根治职责。

### 如何验证和回滚

- 静态检查顶层 `globalProps/env/settings` 和 render 中的普通实例依赖。
- trace 不只数 `__temp`，还要对比具体字段的首轮输入、后续 `UpdateComponentData`、时间顺序和风险提示。
- 分平台回归首屏、滚动、弹层、键盘和核心跳转。
- 灰度观察 JS error、白屏、渲染失败、核心点击和用户反馈。
- 平台分支和规则均保持可开关，出现回归可回到原链路。

## Markdown Repair 方案决策表

### 原文正则还是 AST + 规则

- 原文正则实现快，但没有上下文，会误伤 Code、HTML、Latex、表格和命令行。
- 完整语法分析准确但成本高；项目已有 parser AST，可直接复用结构信息。
- 最终选择 AST 限定候选 Text 节点，再用正则识别局部异常：结构和效率更平衡。

### 前置 Repair 还是后置 Repair

| 问题 | 方案 | 原因 |
| --- | --- | --- |
| 标记完整但 parser 过严 | 前置，调整解析边界 | 不改原文语义，如中文标点包裹加粗 |
| 确定缺空格/空行 | 后置，最小编辑后重解析 | 修复方式确定，如标题、列表、表格 |
| 结构残缺、语义不确定 | 不自动修或降级展示 | 强补可能改变模型原意 |

### 主线程异步还是 Worker

- 文本短、规则少：首屏后低优先级异步执行，接入和调试成本低。
- 长文本、规则增长并出现长任务：迁到 Worker，主线程只做渲染和结果替换。
- Worker 的代价是 AST/文本序列化、取消旧任务、版本管理和 parser 运行环境兼容。

### 规则 Repair 还是在线 LLM Repair

- 确定性规则延迟低、成本低、可灰度回滚，适合线上主链路。
- LLM 理解复杂语义更强，但有延迟、成本和改写原意风险。
- 最终线上使用规则，LLM 只做离线 double-check 和 badcase 分析。

### 为什么关注 Worse 而不只看成功率

Fail 只是没改善，Worse 会破坏原本正确的内容。技术选型因此优先保守和可回滚，宁可低置信不修，也不追求表面覆盖率。

## 收尾句

> 这两项经历的共同点是：我不是只修表面 badcase，而是先建立可观测证据和问题分类，再按数据边界或语法边界分层治理，最后用线上指标和负向指标验证方案。




## 本次 AI 模拟面试：详细示例回答（原文）

### 第 2 题：字节实习中的首屏稳定性治理

我改完之后没有只靠肉眼看，而是按“复现前后对比 + trace 证据 + 回归验证”来确认的。

具体到 PopToast，我先保留了修改前的一组 trace 和录屏，复现条件尽量固定在同一个 Harmony 版本、同一个落地页入口，主要看冷启动首次进入时 toast 容器的首帧表现。修改前比较明显的现象是，trace 里 PopToast 相关的样式判断字段会先出现 `__temp` 占位，首帧使用的是默认 bottom 或空样式；随后后台 React 线程完成计算后，通过 `UpdateComponentData` 把真实值 patch 回来，所以录屏上能看到 toast 容器从默认位置跳到正确位置。

改完之后我重点看了三件事。第一是 trace 链路里同一个字段还会不会从 `__temp` 走到 `UpdateComponentData`。我把原来在 render 阶段隐式依赖实例方法或全局运行时值的地方，改成在真正使用点懒读取，或者由父层通过 props 显式传入。再跑 trace 的时候，PopToast 这块参与布局的值在首帧就能拿到一致输入，不再出现原来那种先占位、再回写导致布局变化的链路。

第二是看视觉回归。我当时会反复冷启动进入页面，观察首屏录屏和帧序列，重点看 toast 容器是否还会先空白占位、再突然跳到正确位置。修改后这一类跳变在我负责的复现场景里没有再出现，表现是首帧位置和后续稳定状态基本一致。

第三是看治理后的回归范围。因为 PopToast 是共享组件，我没有只看这一个页面，还顺带看了几个使用这个组件的入口，确认改成 props 显式化和使用点懒读取之后，没有影响正常展示、点击和消失逻辑。上线前也会结合 QA 回归和灰度 trace 看相关报错有没有新增。比较准确地说，我这里拿到的主要是组件级和灰度期的验证证据，不是我个人独立负责一整套线上大盘指标；但从我负责的链路看，原来 PopToast 对应的首屏 `__temp` / `UpdateComponentData` 回写告警消失了，录屏里的位置跳变也没有复现。

我对这个问题的理解是，关键不是完全消灭后续更新，而是首屏直出和后台线程对同一段 UI 的输入要一致。后续我也把这类问题总结成了一个排查习惯：看到 Harmony 首屏闪白或跳变时，不先默认是接口慢或图片慢，而是先看 trace 里有没有 `__temp` 占位和回写；如果是隐式运行时依赖，就优先做 props 显式化、必要 state 化，或者把顶层缓存改成使用点懒读取，避免同类问题在其他共享组件里反复出现。

### 第 3 题：组件 props 显式化与 state 化的工程判断

可以，我刚才讲得有点偏原则了，我拿 TabPage 里的 tabList 和 activeTabKey 这条链路具体说一下。改造前 TabPage 不是完全由父组件把首屏数据整理好再传进来，它内部会兜底去读一层运行时缓存，类似根据页面类型从 runtime cache 里取 tab 配置；同时父组件里也有一个编译后的中间值，trace 里能看到类似 __temp_tabInfo 这样的变量被传给 TabPage。问题在于 Harmony 首屏阶段这个值不是稳定同步可得的，所以 trace 里表现出来是：第一次 UpdateComponentData 到 TabPage 的时候，tabList 还是空数组或者 undefined，activeTabKey 也是 undefined；随后缓存或父层数据补齐后，又触发第二次 UpdateComponentData，把真实的 tabList 和 activeTabKey 回设进去。这个过程在 UI 上就容易表现成首屏先没有 Tab 区域，或者高度按空数据算了一次，后面内容补进来之后再撑开，造成闪白、内容后补或者布局跳变。后来我做的改法是把 tabList 和 initialActiveTabKey 这类会影响首屏结构的数据全部显式 props 化：在父容器里根据 pageType、query 参数和默认配置先整理出一个稳定值，比如 tabList 至少有默认 tabs，initialActiveTabKey 如果取不到就落到第一个 tab，然后通过 props 传给 TabPage。TabPage 内部不再在 render 链路里自己读 runtime cache，也不再用 props 为空时再异步补读的逻辑。这样改完以后，trace 里我关注的点是 TabPage 的 UpdateComponentData 不再出现“第一次空值、第二次真实值”的形态，而是第一次传入时 tabList 和 activeTabKey 就是完整的，后面只有用户交互或者真实业务刷新才会更新。至于 state，我当时没有把所有字段都塞进 state。我的判断是，像 tabList、初始 activeTabKey、页面类型这种决定首屏结构的数据，应该由父组件显式传入；而 currentActiveKey 这种用户点击 tab 后会变化、并且需要驱动局部 UI 更新的值，才放到 TabPage 自己的 state 里。也就是说 state 的初始值来自 props，但后续点击切换由组件自己维护；如果父层真的切换了页面类型，再用明确的 props 变化去同步，而不是依赖内部兜底读取。验证上，我主要做了三件事：第一是对比改造前后的 trace，看 TabPage 这条链路里空值到有效值的二次 UpdateComponentData 是否消失；第二是在 Harmony 真机和低端设备上录屏看首屏，确认 Tab 区域不再先空白再补出来；第三是看相关组件的日志和截图 diff，确认首屏高度和内容顺序稳定。这个点我没有单独拿到一个非常独立的线上量化指标，但从 trace 链路上看，原来导致回设风险的那条“首轮空数据渲染、后续回填”的链路是被切断了。

### 第 9 题：模糊问题中的主动推进

我讲一个在字节实习时做豆包爱学 Markdown 渲染质量治理的经历。这个事情一开始其实挺模糊的，因为大家看到的现象只是：大模型生成的内容在前端渲染出来有一些异常，比如标题层级不对、列表断开、表格渲染错位、加粗符号残留之类的问题。但问题边界并不清楚，它可能是模型输出本身不规范，也可能是 Markdown 解析器容错不够，也可能是我们前端渲染链路里的规则处理有问题。所以最开始不是一个很明确的“改某个 bug”的任务。

我当时负责的是把这类渲染异常先收敛成一个可分析、可评估、可持续治理的问题，而不是只靠人工看 case。我的第一步不是直接改代码，而是先和相关同学一起把线上和测试样本里的异常 case 拉出来做分类。我把高频问题先归成几类，比如标题、列表、表格、加粗、代码块这些，然后看每一类异常在 AST 上有什么共同特征。这样做的原因是，如果只看最终 HTML，很容易只修表象；但从 AST 上下文看，可以更准确地区分它到底是结构缺失、嵌套错误，还是符号没有闭合。

第二步，我主动补了一套评价口径。因为这个场景最怕的是“修了一个 case，又把另一个正常 case 修坏”。所以我没有只看修复后的命中数量，而是把结果分成 Success、Fail 和 Worse。Success 是原来异常被修好，Fail 是没修掉，Worse 是原来正常或可接受的内容被我们修坏了。这个口径定下来之后，大家讨论规则的时候就不会只凭感觉，而是可以围绕误修风险做取舍。

第三步是在实现上，我把规则分成前置 Repair 和后置 Repair。前置 Repair 主要处理一些比较确定的语法缺失，比如明显的闭合符号问题；后置 Repair 则更多结合解析后的 AST 上下文判断，比如列表和表格的结构是否符合预期。这里我做了一个取舍：没有把规则写得特别激进，而是优先覆盖高频、确定性强的异常。因为这个业务里内容是模型生成的，输入非常开放，如果规则过强，短期看异常率可能下降，但长期会带来更难排查的误修。

推进过程中我也会把每轮规则的效果同步给相关同学，包括新增修复了哪些 case、有没有引入 Worse、哪些 case 暂时不适合前端修。对于明显属于模型输出质量的问题，我会把样本和分类反馈出去；对于前端可以兜底的问题，我就继续在渲染链路里做修复。这样边界是逐步澄清出来的，不是一开始就完全定义好的。

最后结果上，这套治理把 Markdown 渲染异常率从 7.64% 降到了 3.23%，相对下降了 57.72%。除了数据下降，更实际的收益是后面遇到新的渲染问题时，我们不再是临时 patch，而是可以先归类、看 AST 特征、跑 Success/Fail/Worse，再决定是否进入规则库。

我对这件事的复盘是，模糊问题里最重要的不是一上来就证明自己能写很多代码，而是先把“什么算问题、怎么衡量好坏、哪些归前端处理、哪些应该反馈到上游”定义清楚。尤其像大模型内容渲染这种输入不稳定的场景，指标和回归机制比单个修复更重要。这个经验对我后面做前端稳定性、兼容性问题也挺有帮助的。

