# 本轮模拟面试复盘：必修口述版

## 这次真正暴露的问题

不是知识点数量不够，而是回答经常停在“原则层”：

1. 没有先用一句话给问题定性，面试官要从停顿和术语里自己找重点。
2. 项目回答缺一个具体组件、字段和时间顺序，证据链不够硬。
3. 讲了方案，却没有稳定落到验证方式、量化结果和个人负责边界。
4. 监控、Agent、业务系统回答更像功能清单，没有沿一次真实请求/错误/点击跑完端到端链路。
5. 被追问后继续重复原理，没有切换到“改造前 -> trace/接口表现 -> 改造后 -> 如何确认”的具体层。

本轮训练目标：每个项目先练一条 90 秒主链，再准备一条能扛三轮追问的字段级证据链。

## 必修一：数据回设

### 一句话定性

> 数据回设不是接口慢，而是主线程首帧直出和后台 React 线程对同一段 UI 使用了不一致的输入：首帧先按默认/临时值渲染，随后真实值通过 `UpdateComponentData` 回写，造成闪白、内容后补或布局跳变。

### 固定回答顺序

```text
现象：Harmony 冷启动首屏闪白/跳变
-> 范围：我负责哪个组件、哪个字段
-> 证据：首轮 __temp/默认值，后续 UpdateComponentData 真实值
-> 排除：接口日志、图片、样式加载均正常
-> 根因：顶层 runtime 早读，或 render 内隐式依赖
-> 改法：使用点懒读取 / 父层整理后 props 化 / 必要 state 化
-> 验证：同条件 trace 前后对比 + 真机录屏 + 共享入口/QA/灰度回归
-> 边界：没有独立大盘指标就明确说组件级证据
```

### 追问时必须讲出的判断

- 默认值只能防崩，不能阻止真实值后续回写。
- 懒读取解决“初始化太早锁住旧快照”，不等于解决所有异步数据。
- 首屏结构数据由父层整理并 props 化；真正随交互/生命周期变化且驱动 UI 的值才进 state。
- `__temp` 是风险线索，不是单独定罪证据；要和两次更新、运行时提示、视觉现象对上。
- 没有真实字段和改造前后链路时，不要继续泛讲 React 数据流。

### 真实性待确认

模拟示例用了 `PopToast.bottom/visible` 和 `TabPage.tabList/activeTabKey`。请对照真实代码、MR 或 trace：

- 若字段真实：补上实际源码来源、首轮值、第二轮值和改动位置。
- 若字段不真实：换成自己确实改过的一条字段链，保留回答结构。
- 若只能确认组件不能确认字段：面试时诚实说到组件级，不虚构变量名。

完整口述稿见 `05-字节实习两条主线.md`。

## 必修二：rrweb 的亮点

### 一句话定性

> rrweb 的亮点不是“能录屏”，而是把错误事件和现场回放串成可查询链路：`eventId` 找单次错误，`fingerprint` 聚合同类 issue，`sessionId` 找会话，`replayOffset` 找会话内错误时间点。

### 固定回答顺序

```text
SDK init：生成 sessionId，启动 rrweb，记录 replayStartTime
-> TypeError：ErrorBoundary/window.onerror 捕获；Promise 拒绝走 unhandledrejection
-> 立即生成 eventId，记录 timestamp 和 replayOffset
-> normalize 错误，生成 fingerprint
-> payload 入队，有限重试和优先级丢弃
-> 服务端 SourceMap + fingerprint 聚合
-> eventId 查到 sessionId + offset
-> 读取错误前后 rrweb 分片
-> Replayer seek 到错误点并标 marker
```

### 四个标识绝不能混

| 标识 | 粒度 | 用途 |
| --- | --- | --- |
| `eventId` | 一次错误实例 | 查询单条错误详情 |
| `fingerprint` | 一类错误 | 聚合发生次数、影响用户和版本 |
| `sessionId` | 一次页面会话 | 查询该用户现场的 rrweb 分片 |
| `replayOffset` | 会话内时间点 | 定位错误发生前后的回放窗口 |

### rrweb 原理回答

> rrweb 不是视频录制。它先做 FullSnapshot 序列化 DOM，再用 IncrementalSnapshot 记录 mutation、鼠标、滚动、输入和视口变化；Replayer 重建初始 DOM 后按时间应用增量事件。工程亮点在分片、索引、错误关联、seek、脱敏和性能预算。

### 取舍必须和链路一起讲

- 致命错误优先保留；普通行为/性能按 session 采样。
- 队列满时丢低价值行为，不先丢错误和错误窗口。
- 只对网络错误、429、部分 5xx 有限重试；4xx 不盲重试。
- 输入和敏感 DOM 默认 mask，请求数据走白名单/模板化。
- 高频事件节流、分片压缩、队列和录制长度设上限；必要时 Worker。
- 高敏页面禁用或强 mask，并设置保存期限、访问权限和审计。

完整口述稿见 `07-监控SDK与rrweb.md`。

## 其他题目：只修一个核心问题

| 题目 | 本轮失分点 | 下次开口方式 |
| --- | --- | --- |
| 自我介绍 | 经历多但重点不够前置 | 先说前端 + AI 工程化定位，再抛 ReactLynx、Markdown 指标、监控链路三个钩子 |
| AI Agent 架构 | 原回答缺少 Host 调度与数据闭环 | 先讲初版缺陷，再讲 `next_agents` 真调度、结构化终止、混合记忆和三类集合 |
| 页面慢排查 | 背加载流程，没有指标决策 | TTFB/waterfall 判网络，FCP/LCP 判关键内容，TBT/Long Task/INP 判主线程，CLS 判稳定性 |
| RAF 流式渲染 | 没说清“不用会怎样” | 每 chunk setState -> Markdown 重算/commit/滚动掉帧；buffer + RAF 只合并 UI 更新，不改变网络吞吐 |
| 企业业务系统 | 模块化原则多，接口契约少 | 跑一次“按钮展示 -> 确认置灰 -> transition API -> 后端原子校验 -> 返回最新状态 -> 刷新/冲突处理” |
| 模糊问题推进 | 没突出主动定义问题 | 用 Markdown 治理：先分类和定义异常率/Success-Fail-Worse，再做 AST Repair 和跨团队闭环 |
| 反问 | 可以更贴岗位 | 问当前核心挑战；再问校招生 3-6 个月的交付预期 |

## 项目题统一口述模板

> 先给结论：这是一个什么问题，我负责什么。然后讲一个具体对象：改造前是什么输入和现象，我用什么证据定位根因。接着讲候选方案与取舍、我实际改了什么。最后给改造前后验证、量化结果和个人边界。如果没有线上指标，就说真实的本地、QA、灰度或离线证据。

被打断说“还是太原则”时，立刻切换：

> 好，我不再讲原则，拿一个组件/一次请求/一条错误的具体时间线说。

## 练习方式

1. 数据回设和 rrweb 各录一遍 90 秒，禁止看稿。
2. 回听时圈出“这个、然后、就是、的话、呃”，每句话只保留一个结论。
3. AI Agent 录一遍 2 分钟，只允许讲“初版问题 -> 状态机 -> 调度闭环 -> 记忆分层 -> 当前边界”。
4. 第二遍只练面试官追问：具体字段是什么、证据在哪、改前改后怎样、如何验证。
5. 第三遍随机从中间开始答，避免只会从头背。
6. 最后逐项核实示例字段、数字和实现状态；宁可少说，也不把 AI 示例说成真实事实。
