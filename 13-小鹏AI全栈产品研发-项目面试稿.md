# 小鹏 AI 全栈产品研发：项目面试稿

## 一句话定位

> 这是一个面向自主学习者的 AI 兴趣教练。我从目标拆解和计划调整场景出发，独立完成 React 前端、Modern.js BFF、计划工具、多 Agent 编排、Mongo/Redis、长期记忆、OIDC、监控和容器化配置；项目重点是把模型的不确定输出放入可验证、可降级的工程闭环。

## 90 秒项目介绍

> 我做的是一个 AI Agent 兴趣教练，用户可以输入学习目标、时间和限制条件，系统不仅回答问题，还能搜索资料、创建和修改结构化计划。
>
> 产品上我主要解决三个问题。第一，普通聊天给出的计划看起来完整，但缺少可行性和风险检查；第二，“先查资料、再修改已有计划”这类多步骤需求容易只执行一半；第三，长会话中用户的目标和约束容易丢失。
>
> 技术上，前端使用 React 18 和 TypeScript，BFF 使用 Modern.js 和 Hono，数据层使用 MongoDB 与 Redis。复杂规划进入 Planner、Critic、Host、Reporter 四角色流程。模型负责生成和语义检查，Host 用代码状态机决定下一轮实际执行谁：有高风险就让 Planner 修订，修订后必须由 Critic 验证，结构化质量门槛全部通过才结束。过程通过 SSE 推给前端，前端使用 buffer 和 requestAnimationFrame 合并渲染。
>
> 长期记忆方面，MongoDB 原始消息是事实源，切片、Embedding 和摘要都是可重建派生层；查询时用 BM25 和向量召回，经 RRF、时间和重要性重排，再按 token 预算装入上下文。Embedding 或摘要失败不会阻断聊天。
>
> 我对这个项目的复盘是，工程完整度已经比较高，但真实用户调研和计划采用率仍然不足。因此如果继续做，我会优先补用户访谈、行为埋点，以及单 Agent 和多 Agent 的质量、延迟、成本对照，而不是继续堆 Agent 数量。

## 为什么这个项目匹配 JD

| JD 能力 | 项目证据 |
| --- | --- |
| 需求与产品 | 从学习计划、计划调整、多步骤任务出发定义产品问题；PRD 已补齐，用户调研待完成 |
| 前端 | React、TypeScript、流式 Markdown、虚拟列表、Worker、错误和取消状态 |
| 后端 | Modern.js BFF、Hono、REST、MongoDB、Redis、鉴权、限流和队列 |
| AI 产品 | Agent、Function Calling、Hybrid RAG、Schema、评测、降级与成本预算 |
| 数据 | TTFT、TTLB、TPS、token、错误率、数据库和工具指标；业务指标仍需补充 |
| 部署 | Docker Compose、Nginx、Prometheus、Grafana、Jenkins 配置 |
| 独立 Demo | GitHub 公开仓库和完整前后端代码 |

## 五分钟 Demo 讲解顺序

### 0:00～0:30：用户问题

> 用户已经有 IELTS 计划，但备考周期从 4 个月变成 8 个月。他希望系统先找出旧计划，再搜索新的备考建议，然后更新计划。这个任务不是一次问答，而是一个需要历史数据、外部搜索和写操作的完整流程。

### 0:30～1:30：产品流程

输入：

> 列出我原来的 IELTS 学习计划。我的备考周期改为 8 个月，请搜索相关建议，然后更新原计划。

展示：

```text
list_plans
→ search_web
→ update_plan
→ 返回更新后的 PlanCard
```

强调：

- 每个工具结果会进入下一轮模型上下文。
- 最大工具轮次和参数校验防止死循环与非法写入。
- 搜索结果失败时不会伪造来源。

### 1:30～2:30：多 Agent

切到复杂规划模式，展示：

- Planner 初版方案。
- Critic 的有效性检查和高风险。
- Host 的 `revise / challenge / verify / finalize / terminate`。
- 下一轮只执行 Host 指定的 Agent。
- Reporter 披露最终方案与剩余风险。

面试时不要说“相似度高于 0.9 就自动收敛”。当前代码中，相似度只用于检测重复或过快一致；真正的结束条件是可行性、现实性、完整性、覆盖率和未解决高风险。

### 2:30～3:20：流式体验

沿链路讲：

```text
模型字节流
→ BFF TransformStream
→ fetch + ReadableStream
→ TextDecoder(stream: true)
→ buffer
→ requestAnimationFrame flush
→ Markdown/Agent 卡片
```

补充：

- 普通文本按帧批量刷新。
- 结束、错误、工具完成立即 flush。
- `AbortController` 取消旧流。
- 虚拟列表控制长会话 DOM。

### 3:20～4:10：长期记忆

展示数据分层：

```text
messages：原始消息事实源
memory_items：切片、Embedding、全文/向量召回
memory_summaries：目标、偏好、约束和来源 ID
conversation_token_states：输入压力、累计成本、未摘要增量
multi_agent_sessions：短期执行检查点
```

强调：

- 摘要不覆盖原文。
- Embedding 失败降级 BM25。
- 摘要运行或失败时不阻塞用户提问。
- 最近两轮强制保留，其余按价值/token 成本装箱。

### 4:10～5:00：上线与复盘

> 项目有 Docker Compose、Nginx、Prometheus、Grafana 和 Jenkins 配置。Nginx 对 SSE 关闭代理缓冲，Prometheus 记录 TTFT、TTLB、TPS、token 和错误等指标。当前更准确的说法是完成了本地容器化和部署链路配置；除非有可展示的公网环境和流水线记录，我不会把它说成大规模生产上线。
>
> 如果继续迭代，我会先补真实用户访谈、计划确认与完成埋点，并对单 Agent、多 Agent 做任务成功率、P95 和 token 成本对照。

## 高频追问与回答锚点

### 1. 为什么做兴趣教练，而不是普通聊天机器人？

> 普通聊天的输出通常停留在文本建议；兴趣教练需要维护目标、约束和计划状态，还要完成搜索、创建和更新等写操作，因此更适合验证 AI 产品如何进入真实工作流。

### 2. 你怎么证明用户真的需要？

> 当前需求来自我对自主学习和复杂计划场景的观察，技术可行性已经验证，但还不能说用户价值被证明。面试前我会完成 3～5 次访谈；正式迭代会用计划确认率、修改原因、任务开始率和完成率验证。

### 3. 为什么用多 Agent？

> 复杂计划同时包含生成、校验、流程控制和表达，一个 Prompt 容易职责冲突。Planner 和 Critic 分开后能单独测试失败模式；Host 用代码状态机保证轮次、风险和终止可控。简单任务仍走单 Agent，避免不必要的延迟和 token。

### 4. 初版最大的设计问题是什么？

> 初版 Host 虽然返回 `next_agents`，但编排器下一轮仍固定执行 Planner 和 Critic；另外把语义相似度当作主要终止信号。这会造成“看起来有调度，实际上没有执行”，也可能把相似但错误的方案判为收敛。

### 5. 后来怎么修？

> 第一轮固定建立方案和评审，后续严格按 `next_agents` 执行；`must_address` 进入下一 Agent 的输入。有高风险先 revise，Planner 修订后必须 verify。只有可行、现实、完整、覆盖率达标且没有高风险时才能 finalize；达到最大轮次则 terminate 并披露风险。

### 6. 为什么相似度不能代表质量？

> 相似度只能说明两段文本接近，不能证明事实正确、风险关闭或约束满足。它更适合检测重复和首轮过快一致，质量门槛必须来自结构化检查和任务结果。

### 7. 为什么不用 LangGraph 直接实现？

> 项目核心状态机规模有限，而且需要展示 Host 决策、SSE 事件和自定义检查点。自己实现能明确控制每一步，并不是重写模型或解析器。任务图扩大、需要持久化 run 和复杂恢复时，再评估 LangGraph 等成熟框架。

### 8. 为什么选择 BFF？

> Key 保护只是底线。BFF 还统一管理 Session、上下文拼接、模型路由、工具权限、Agent 编排、SSE 协议和错误映射，避免这些能力扩散到 React 组件。

### 9. SSE 为什么不用 WebSocket？

> 主要是服务端单向生成，且需要 POST Prompt、鉴权头和主动取消，所以使用 `fetch + ReadableStream` 解析 SSE 格式。WebSocket 更适合高频双向协作。

### 10. 为什么 buffer + RAF？

> 它不让模型更快，而是将同一帧内多个小 chunk 合并成一次 React 更新，减少 Markdown 重算、commit、layout 和滚动抖动。验证看 commit 次数、Long Task、输入响应和滚动帧稳定性。

### 11. 为什么 MongoDB 保存原文，摘要不能直接替换历史？

> 摘要可能漏掉否定、数字和约束。如果覆盖原文，错误无法恢复。原文是事实源，切片和摘要只做可重建的检索与压缩层。

### 12. 为什么 BM25 和向量都要？

> Embedding 擅长语义改写，BM25 擅长错误码、计划名、路径和明确关键词。两路分数量纲不同，所以先用 RRF 融合排名，再叠加时间和重要性。

### 13. token 怎么算？

> 累计 `total_tokens` 表示成本，不能代表下一轮上下文大小。项目分开记录上一轮实际输入、累计计费量和上次摘要后的新增原文；构建请求前还会预留输出空间和安全边界。

### 14. 模型或记忆服务失败怎么办？

> 模型请求有超时、取消和错误态；Embedding 失败回退 BM25；摘要失败继续查原文；结构化结果解析失败降级纯文本；派生记忆失败不阻断消息落库。

### 15. 如何保证工具调用安全？

> 工具白名单、Zod/参数校验、最小权限、超时、最大轮次、幂等和审计。写操作不能因为模型生成了工具名就直接执行；高风险操作还应增加人工确认。

### 16. 数据量或 QPS 扩大十倍怎么办？

> 先用 trace 分解模型、Embedding、Mongo 和 SSE 持有时间。索引任务放持久队列，本地 BM25 fallback 切到数据库原生 Search；简单问题绕开多 Agent；设置模型、用户和 token 维度的并发预算；检查网关长连接数、超时和断开取消。

### 17. 你如何做产品数据分析？

> 技术层已有 TTFT、TTLB、TPS、token、错误率和数据库指标。产品层会补计划确认、修改、开始、完成和回访事件，再按单/多 Agent、模型、任务类型做漏斗和质量成本对比。

### 18. 项目真正上线了吗？

> 当前有完整本地运行、Docker Compose、Nginx、监控和 Jenkins 配置；如果没有持续公网环境和真实流量，我会准确说成“完成本地容器化和部署链路”，不说成大规模生产上线。

## 真实证据边界

### 可以放心说

- React + Modern.js BFF + Hono + MongoDB + Redis。
- OIDC Authorization Code + PKCE、BFF Session、CSRF 和限流。
- SSE 流式输出、取消、重试、队列和虚拟列表。
- Planner、Critic、Host、Reporter 动态调度。
- BM25 + 向量 + RRF + 时间/重要性重排。
- 原文事实源、派生摘要、来源消息 ID 和 token 双账本。
- Prometheus/Grafana 配置和技术指标。
- Docker Compose、Nginx、Jenkins 配置。

### 需要限定

- “企业级”：更准确说“按企业场景补齐了鉴权、限流、监控和部署边界”，不代表经过企业生产流量验证。
- “生产上线”：没有公网运行证据时说本地容器化和部署配置。
- “用户价值”：没有访谈、留存、任务完成数据时说产品假设。
- “性能提升”：只说有对照测量的指标；不要使用旧文档中无法复现的 300%、80%、60% 等数字。
- “向量相似度收敛”：当前版本不是只靠相似度。
- “向量数据库预留”：当前版本已有混合召回代码和建索引脚本，但数据库原生索引是否执行需按实际环境说明。

### 不能说成已实现

- 大规模真实用户和商业收入。
- 成熟企业身份平台。
- 完整持久化 run、工具级审计平台和生产任务队列。
- 已经验证的计划完成率、留存和满意度。
- 所有依赖漏洞已治理。

## 面试前 2 天项目修复优先级

### P0：必须完成

1. 更新 README 中过时的 Host 收敛规则和记忆描述。
2. 修复旧单测：Function Calling 已替代 `<tool_call>` 文本协议。
3. 修复 `postbuild-optimize-assets.js` 的跨平台路径：

   ```js
   const cssPath = join('dist', href.replace(/^\//, ''));
   ```

   不要强制把 `/` 替换成 `\`。

4. Dockerfile 不要复制未提交的 `.env.production`，改成运行时环境变量注入。
5. 在干净克隆中重新执行：

   ```bash
   npm ci
   npm run test:unit -- --runInBand
   npm run build
   ```

### P1：强烈建议完成

1. 补 3～5 个目标用户访谈。
2. 补一段 90 秒 Demo 录屏。
3. 给 README 增加“当前能力 / 规划能力 / 已知边界”。
4. 保存一次 Grafana、测试和 Docker 运行截图。
5. 准备固定三步工具链和多 Agent 修订验证两个演示输入。

### P2：有时间再做

1. 补计划确认、修改原因和任务完成埋点。
2. 建立 20～30 条固定任务回归集。
3. 对单 Agent、多 Agent 比较完成率、P95 和 token。
4. 分级处理 `npm audit` 结果，不直接执行破坏性升级。

## 面试收尾

> 这个项目让我意识到，AI 全栈产品不是把模型接到页面上就结束。真正困难的是先明确用户任务，再把模型输出变成有状态、可验证的产品流程，同时处理权限、失败、成本和数据闭环。当前项目已经完成了工程闭环，下一阶段最重要的是用真实用户和业务指标验证，而不是继续增加更多技术名词。
