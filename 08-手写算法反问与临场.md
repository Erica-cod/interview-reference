# 手写、算法、反问与临场

## 本次 AI 模拟面试：详细示例回答（原文）

### 第 10 题：补充信息与反问

我想补充一个刚才可能没有完全展开的点：我觉得自己和这个岗位比较匹配的地方，不只是 React 或 TypeScript 这些技术栈本身，而是我比较习惯把一个前端问题做成闭环，包括定位问题、拆方案、和后端或上下游对齐、最后用数据验证。

比如我在字节做 Markdown 渲染质量治理时，最开始不是直接写一堆规则去修，而是先看异常主要集中在哪些结构上，比如标题、列表、表格、加粗这些高频场景；然后基于 AST 上下文做检测，再把修复拆成前置 Repair 和后置 Repair，并且用 Success、Fail、Worse 这样的评测结果来控制误修。最后异常率从 7.64% 降到 3.23%。这个经历让我比较深地意识到，前端工作不只是把页面做出来，很多时候还要保证复杂输入、复杂状态下的稳定性和可验证性。

我理解禾赛这边前端会服务研发、制造、销售等业务系统，可能会有很多流程型页面，比如数据录入、审批、状态流转、权限控制、前后端接口联调这些。我之前在微前端、稳定性治理、监控 SDK 和 BFF 项目里都有类似的工程经验，所以如果有机会加入，我希望能先把业务流程和系统边界理解清楚，然后在页面开发之外，也参与到性能、稳定性、工具链或者组件沉淀这些事情里。

我也想请教两个问题：第一，禾赛前端团队现在在生产、销售或者研发系统里，最核心的前端挑战更多是复杂业务流程和权限状态，还是性能、数据展示和系统稳定性？第二，对于校招生前端，团队通常希望前三到六个月能够承担到什么程度，是先以业务需求交付为主，还是也会参与一些基础建设和工程化优化？


## 手写前先确认

- 输入类型、空数组、重复键如何处理。
- 是否允许修改原数据。
- 需要保留第一次还是最后一次。
- 数据量和时间/空间要求。

## 1. 对象数组去重

```js
function uniqueById(list) {
  const map = new Map();
  for (const item of list) map.set(item.id, item);
  return [...map.values()];
}
```

`O(n)` 时间、`O(n)` 空间，保留最后一个。

## 2. 防抖

```js
function debounce(fn, delay) {
  let timer = null;
  return function (...args) {
    clearTimeout(timer);
    timer = setTimeout(() => fn.apply(this, args), delay);
  };
}
```

追问：leading/trailing、取消、异步返回值、组件卸载清理。

## 3. 并发控制

```js
async function mapLimit(items, limit, worker) {
  const result = new Array(items.length);
  let next = 0;

  async function run() {
    while (true) {
      const index = next++;
      if (index >= items.length) return;
      result[index] = await worker(items[index], index);
    }
  }

  await Promise.all(
    Array.from({ length: Math.min(limit, items.length) }, run),
  );
  return result;
}
```

追问：失败是立即终止还是收集结果；需要 AbortSignal、重试和顺序保证吗？

## 4. LRU Cache 思路

- Map 保持插入顺序。
- `get` 命中后 delete + set 移到最新。
- `put` 超容量时删除 `map.keys().next().value`。
- `get/put` 平均 `O(1)`。

## 代码输出题检查顺序

1. 变量提升和暂时性死区。
2. `this` 的调用位置，箭头函数是否捕获外层 this。
3. 同步代码。
4. 微任务：Promise/queueMicrotask。
5. 定时器等后续任务。
6. 闭包读取的是哪次渲染/哪次循环的绑定。

## 手写题中的方案取舍

### 去重：Map、Set、排序还是双循环

- 原始值去重：`Set` 最直接。
- 对象按 key 去重：`Map` 能明确表达 key 冲突和保留首项/末项。
- 允许改变顺序且内存受限：排序后相邻扫描，时间 `O(n log n)`。
- 双循环/`findIndex` 通常 `O(n²)`，只适合极小数据或展示直观基线。

答题时要主动说明：是否保持顺序、冲突保留谁、key 缺失怎么办。

### 防抖、节流还是 RAF

| 方案 | 行为 | 典型场景 |
| --- | --- | --- |
| 防抖 | 停止触发一段时间后执行 | 搜索框、异步校验 |
| 节流 | 一段时间内最多执行一次 | 滚动统计、拖拽同步 |
| RAF | 下一帧绘制前最多合并一次 UI 更新 | 动画、流式文本、位置更新 |

追问 leading/trailing 时，先定义第一次是否立即执行、最后一次是否补执行，再写状态变量；不要只改 setTimeout。

### 并发任务失败策略

- `Promise.all`：任一失败立即 reject，适合必须全部成功的原子任务。
- `Promise.allSettled`：收集每项结果，适合批处理和部分成功。
- 并发池：限制同时执行数量，避免连接、内存或下游接口被打满。
- 重试只针对可重试错误，增加最大次数、退避和 jitter；任务最好支持 AbortSignal。
- 若要求输出顺序与输入一致，按 index 写入结果，而不是按完成顺序 push。

### 原地修改还是返回新数据

- 原地算法空间更省，但会改变调用方数据，调试和 React 状态更新更危险。
- 返回新结构更安全、可预测，代价是分配和拷贝。
- 面试先问是否允许修改输入；React 状态默认保持不可变更新。

### 递归还是迭代

- 递归表达树/回溯更直观，但深度大可能栈溢出。
- 迭代需要显式栈/队列，控制力更强，适合深树和生产边界。
- BFS 天然用队列，适合层序/最短无权路径；DFS 适合路径探索和回溯。

### 普通对象还是 Map

- key 明确为字符串、需要 JSON 序列化：普通对象简单。
- key 类型任意、需要稳定 size/迭代顺序和频繁增删：Map 语义更合适。
- 对象要警惕原型键；可用 `Object.create(null)`，但可读性通常不如 Map。

## 手写时如何讲取舍

1. 先给最清晰可正确的方案和复杂度。
2. 再说替代方案，以及它在什么约束下更好。
3. 写完检查空输入、重复值、越界、异步失败和是否修改参数。
4. 如果面试官要求优化，先确认优化目标是时间、空间、稳定性还是可读性。

示例：

> 我先用 Map 做 `O(n)` 去重并保持原顺序，空间是 `O(n)`。如果题目严格限制额外空间且允许改变顺序，可以先排序再扫描，代价是时间变成 `O(n log n)` 并破坏原顺序。

## 两天内只刷这些算法类型

- 两数之和、对象/数组去重。
- 有效括号。
- 合并两个有序链表、反转链表。
- 二分查找。
- 最大子数组和。
- 无重复字符最长子串。
- 层序遍历。
- 快排思想与复杂度。

先保证能写、能讲复杂度和边界，不追求困难题数量。

## 三个高质量反问

优先问两个，避免像念问题清单：

1. 禾赛前端现在服务研发、制造或销售系统时，最核心的挑战更偏复杂业务流程与权限状态，还是性能、数据展示和系统稳定性？
2. 对校招生前端，团队希望前三到六个月承担到什么程度：先以业务需求交付为主，还是也会参与组件、工程化或稳定性建设？

如时间允许再问：

- 新人通常会先进入哪类业务，如何完成领域知识和系统边界的学习？
- UmiJS/Ant Design 之外，团队的状态管理、测试和发布链路是什么？
- 生产系统是否有大表格、实时数据或可视化场景？

## 行为题：模糊问题如何主动推进

优先讲 Markdown 渲染质量治理，因为它有真实指标和清晰闭环：

> 当时最初只有持续出现的 Markdown badcase，但“什么算异常、哪些该由前端修、怎样证明没修坏”都不够清楚。我先抽样分类标题、列表、表格和加粗问题，定义异常率以及 Success、Fail、Worse 口径；随后利用 parser AST 定位疑似失败的 Text 节点，用上下文排除 Code、HTML、Latex 等不该修的区域。对 parser 边界过严的问题做前置 Repair，对原文确定缺空格/空行的问题做最小后置 Repair，低置信 case 不自动改。每轮把修复样本、Worse 和模型侧问题同步给上下游，逐步明确前端与模型的边界。最终异常率从 7.64% 降到 3.23%，也沉淀了后续 badcase 的分类、评测和准入流程。

这题要突出的是“我先定义问题和评价口径，再推动边界与方案落地”，不是只介绍 AST/正则实现。

## 不会时的安全回答

> 这个 API 细节我现在不能保证准确。我确定的是它要解决……，边界包括……。如果落地，我会先查官方文档/做最小实验，再通过……指标验证。

## 项目被质疑时

- 不争辩，先澄清自己的真实负责范围。
- 能展示证据：数据口径、方案对比、失败案例、回滚方式。
- 如果实现不是本人主责：说清理解，不虚构代码细节。
- 如果数据来自离线实验：明确样本数和模拟边界。

## 面试最后 30 秒

> 今天交流后，我对岗位承接研发、制造和销售系统的业务更感兴趣。我的优势是 React 工程实践、稳定性治理和独立项目闭环；UmiJS/Ant Design 的生产经验是我需要快速补齐的部分，但底层能力和迁移路径比较明确。感谢您的时间。
