# AI 时代 Coding 成长历程

## 一、成长主线

```text
Vibe Coding → 项目变大、耦合和回归增加
→ Spec Coding → TDD + SDD → Agent Skill
```

我的核心变化：

> 从“让 AI 帮我写代码”，转变为“我设计约束、验证体系和工作流，让 Agent 在可控边界内完成工程任务”。

## 二、第一阶段：Vibe Coding

最开始使用 AI Coding 时，我主要采用聊天式开发：

典型过程是“描述需求 → AI 生成 → 发现问题继续追问 → 不断补 Prompt 和局部修改”。

这个阶段的优点：

- 原型速度快。
- 可以迅速尝试陌生技术。
- 小功能和一次性脚本开发效率高。
- 能降低从想法到可运行 Demo 的门槛。

但项目扩大后开始出现问题：

- AI 只看到局部上下文，容易重复实现已有能力。
- 一个需求可能同时修改 Handler、Service、Store 和组件，耦合越来越严重。
- Prompt 中的临时决定没有沉淀，换一次会话就丢失。
- 开发速度看似很快，后期阅读、修改和回归成本越来越高。

## 三、第二阶段：Spec Coding

为了解决 AI 只根据局部对话写代码的问题，我开始在编码前先整理 Spec。

一份最小 Spec 包含：

```text
背景与问题
目标和非目标
当前实现
涉及模块
数据结构和接口
异常与降级策略
兼容性要求
验收标准
```

例如不再只说“优化 RAG 召回”，而是先定义：目标是解决分路 Top K 的证据丢失；原始消息仍是事实源；方案包含统一候选池、摘要回源、邻居扩展和动态 Top K；指标包括 Evidence Recall、冗余 Token、P95 和 Token Budget；本阶段不引入 Query Rewrite 与 Cross-Encoder。

Spec Coding 带来的变化：

- AI 在统一目标下修改代码，而不是不断打补丁。
- 可以提前发现模块边界和数据流问题。
- 验收标准从“感觉能用”变成可检查结果。
- 多轮对话和不同 Agent 之间可以共享同一份上下文。
- 需求变化时可以判断哪些设计和测试需要同步更新。

但只有 Spec 仍然不够，因为文档可能正确，代码实现却可能发生回归。

## 四、第三阶段：TDD

实习中我进一步使用 TDD，也就是 Test-Driven Development：

```text
Red：先写失败测试，明确预期行为
Green：实现满足测试的最小代码
Refactor：在测试保护下整理结构
```

TDD 对 AI Coding 的价值尤其明显：

- 测试把自然语言需求变成可执行约束。
- Agent 是否完成任务不再只靠人工阅读。
- 修改复杂代码时能及时发现旧功能回归。
- 边界条件、错误分支和幂等行为可以提前固化。
- 重构时更有信心，不需要完全依赖 Agent 的解释。

优先测试核心业务规则、状态机、幂等与并发边界、召回排序、Redis 限流以及已出现过的线上 Bug。避免为测试写特例、只断言内部实现、盲目追求覆盖率，或者让 Agent 同时随意修改实现与验收标准。

我的原则：

> 测试可以由 Agent 辅助生成，但关键断言和业务边界必须由人确认。

## 五、第四阶段：SDD

这里的 SDD 指 Spec-Driven Development。

Spec Coding 更像“编码前先写说明”，SDD 则把 Spec 提升为整个开发过程的事实源：

流程是“需求变化 → 更新 Spec → 拆分设计与任务 → 建立测试映射 → Agent 实施 → 验证结果回写”。

SDD 重点管理：

- 为什么做。
- 做到什么程度。
- 哪些内容明确不做。
- 模块和接口如何变化。
- 风险、迁移和回滚方式。
- 每条需求由什么测试或指标验收。

TDD 与 SDD 的关系：

```text
SDD：保证方向、边界和系统设计正确
TDD：保证具体行为可以被执行和验证
```

可以理解为：

> Spec 是人和 Agent 的共同契约，测试是这份契约中可以自动执行的部分。

## 六、Agent Skill：把方法沉淀为实现手段

当流程逐渐稳定后，我不再每次从头给 Agent 写长 Prompt，而是把重复方法沉淀成 Agent Skill。

Skill 可以包含：

- 适用场景和触发条件。
- 必须遵守的工程约束。
- 标准执行步骤。
- 代码模板和参考实现。
- 测试、检查和验收命令。
- 常见风险及禁止事项。

例如可以沉淀 RAG 召回评测、Redis 限流压测、慢 SQL 排查、SSE 测试和代码审查 Skill。

Agent Skill 的价值：

- 把个人经验变成可重复执行的工程流程。
- 降低每次 Prompt 遗漏关键步骤的概率。
- 让不同 Agent 使用相同规范、模板和验收方式。
- 把“如何做”从聊天记录中抽离，形成长期资产。
- 新成员可以通过 Skill 更快理解团队开发方法。

Skill 不是让 Agent 自主决定一切：

> 人负责目标、边界、关键取舍和最终验收；Agent 负责检索上下文、执行步骤、生成实现、运行测试和整理结果。

## 七、我现在的开发流程

```text
1. 理解需求和现有代码
2. 写 Spec：目标、非目标、接口、风险、验收标准
3. 拆成可以独立验证的小任务
4. 先补失败测试或回归样例
5. 调用对应 Agent Skill 实现
6. 运行单测、集成测试、类型检查和专项压测
7. 检查 Diff、边界条件和兼容性
8. 更新 Spec、文档和经验 Skill
```

这套流程的核心不是追求让 AI 写更多代码，而是：

- 降低错误修改的概率。
- 控制大型项目中的耦合。
- 让关键决策可以追溯。
- 让开发结果可以重复验证。
- 把一次解决问题的经验沉淀为下一次的起点。

## 八、完整面试说辞

> 我使用 AI Coding 的方式经历了几个阶段。最开始是比较典型的 Vibe Coding，通过不断聊天让模型生成和修改代码。这个方式做 Demo 很快，但项目变大后，我发现 AI 容易只理解局部上下文，一个需求会在多个模块不断打补丁，代码耦合、重复实现和回归问题逐渐增加，而且很多设计决策只存在于聊天记录里。
>
> 后来我转向 Spec Coding，在写代码前先明确背景、目标、非目标、数据结构、接口、异常策略和验收标准。这样 AI 不再只根据最后一句 Prompt 修改，而是围绕统一规格工作。
>
> 实习中我又进一步使用 TDD 和 SDD。SDD 把 Spec 作为需求、设计、任务和验收的共同事实源；TDD 则先把关键行为写成失败测试，再实现最小代码并重构。两者结合后，Spec 控制方向和边界，测试负责提供可执行的正确性证明。
>
> 在实现手段上，我会把稳定的流程继续沉淀为 Agent Skill，例如慢 SQL 排查、Redis 限流压测和 RAG 召回评测。Skill 中不仅有 Prompt，还包含执行步骤、模板、验证命令和风险边界。我的角色也从让 AI 直接写代码，变成设计约束、组织上下文、定义验收并对最终结果负责。

## 九、最后 20 秒

> 我的 AI Coding 成长不是从不用 AI 到使用 AI，而是从无约束地让 AI 生成代码，逐步走向 Spec 驱动方向、TDD 驱动正确性、SDD 管理完整开发过程，再通过 Agent Skill 固化团队经验。AI 提高的是执行效率，工程质量仍然来自清晰边界、可执行测试和人的最终判断。
