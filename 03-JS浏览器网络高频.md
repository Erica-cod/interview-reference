# JavaScript、浏览器与网络高频小抄

## 本次 AI 模拟面试：详细示例回答（原文）

### 第 6 题：浏览器渲染机制与性能优化落地

我一般会把这个问题拆成两层：先看页面从资源请求到可交互经过了哪些阶段，再用指标把慢的问题定位到具体阶段，而不是直接说“前端慢”。

以一个 React 页面为例，用户输入 URL 或点击路由之后，首先是网络阶段，包括 DNS、TCP/TLS、请求 HTML 或前端资源。如果是业务系统，很多时候还会有接口请求和鉴权逻辑。HTML 返回后，浏览器开始解析 DOM，同时请求 CSS、JS、图片、字体这些资源；CSS 会影响渲染树构建，JS 的下载、解析和执行也可能阻塞主线程。等 DOM 和 CSSOM 基本准备好之后，浏览器会做 style calculation、layout、paint，最后合成到屏幕上。对于 React 应用，还会多一层框架运行时的过程，比如 bundle 执行、组件 render、state 更新、事件绑定，用户真正能稳定点击、输入并得到反馈，才算进入可交互状态。

如果用户反馈“页面慢”，我不会先猜原因，而是先按阶段看指标。第一类是网络和后端问题，我会看 TTFB、接口耗时、资源 waterfall，如果 TTFB 很高，或者关键接口返回慢，那前端优化空间有限，需要和后端一起看缓存、查询、网关和接口聚合。第二类是首屏渲染问题，我会看 FCP 和 LCP，比如最大内容是首屏 banner、表格还是一段业务内容，如果 LCP 元素依赖很大的 JS 包或者慢接口，就要考虑拆包、预加载、骨架屏、接口并行或者首屏数据裁剪。第三类是主线程问题，我会看 TBT、Long Task、INP，以及 Performance 里的 flame chart，如果 JS 执行时间很长，说明可能是初始化逻辑、复杂计算、频繁 setState 或者大组件树渲染拖慢了页面。第四类是视觉稳定性，我会看 CLS，判断是不是图片没有宽高、异步数据回填、弹窗或 tab 内容后补导致布局跳动。

结合我自己做过的几个场景，首屏稳定性治理里，我更关注的不是单纯“快不快”，而是首屏有没有闪白、内容后补和布局跳变。当时我会结合 trace 看数据什么时候进入组件、什么时候触发 UpdateComponentData，以及哪些字段是从临时缓存回设到 UI 的。如果发现组件首屏依赖一个不稳定的顶层缓存，我会把它改成使用点懒读取，或者把共享组件的 props 显式化，必要时转成 state，让数据流更可控。验证上不是只看代码改完，而是看 trace 前后首屏更新次数、内容回填时机，以及异常页面的复现情况有没有下降。

在 SSE 流式渲染那个项目里，瓶颈更容易出现在主线程和 React 更新上。因为模型返回是很多小 chunk，如果每个 chunk 都直接 setState，就会造成高频 render，用户看到的可能不是网络慢，而是页面打字卡、滚动不跟手。所以我当时用了 buffer 缓冲队列，把多个 chunk 合并，再用 requestAnimationFrame 批量刷新，让更新节奏跟浏览器绘制节奏对齐。这里我会用 Performance 面板看 long task 有没有减少，用 React Profiler 看组件 commit 次数和耗时，也会观察流式输出时输入框、滚动这些交互是否受影响。

长会话列表则是另一个典型问题。消息越来越多以后，如果所有 DOM 都留在页面上，layout、paint、内存都会变差，滚动也会卡。我在项目里用了 react-virtuoso 做虚拟列表，本质上是只渲染视口附近的消息，把 DOM 数量控制住。判断这里慢不慢，我会看 DOM 节点数、滚动 FPS、内存变化，以及 Performance 里滚动时是否频繁触发布局计算。

所以总结一下，我排查页面慢会先分层：TTFB 和 waterfall 判断网络/服务端，FCP/LCP 判断首屏资源和关键内容，CLS 判断稳定性，TBT/INP/Long Task 判断主线程和交互，再结合 React Profiler、PerformanceObserver、Web Vitals 和线上监控去验证。优化也不会只做一个点，而是根据证据决定是该改接口链路、改资源加载、减少渲染次数，还是做虚拟列表和批量更新。


## `==` 与 `===`

- `===` 不做类型转换；类型不同直接为 `false`。
- `==` 会按规范转换：对象先做 ToPrimitive，字符串/布尔值可能转数字。
- 特例：`null == undefined` 为 `true`；`NaN` 与任何值都不相等；`0 === -0` 为 `true`。
- 工程默认用 `===`，避免不可读的隐式转换。

## 垃圾回收与泄漏

- 主流思路是从全局对象、当前调用栈等根节点做可达性标记，再清除不可达对象。
- 引擎通常配合分代、增量和并发回收降低停顿。
- 常见泄漏：未移除的监听器/定时器、闭包长期引用大对象、无限增长的 Map/缓存、脱离 DOM 仍被 JS 引用。
- 排查：Performance/Memory 面板、Heap Snapshot、Allocation Timeline，对比对象数量与引用链。

## 对象数组按 `id` 去重

```js
function uniqueById(list) {
  return [...new Map(list.map(item => [item.id, item])).values()];
}
```

- 时间 `O(n)`，空间 `O(n)`；保留最后一次出现的同 `id` 元素。
- 要保留第一项，用 `Set + filter`，首次见到时加入集合。
- 先确认去重键是否稳定，以及冲突时保留第一项还是最后一项。

## 从 URL 到页面显示

1. 解析 URL，查缓存/HSTS，DNS 解析。
2. 建立 TCP；HTTPS 还要 TLS 握手。
3. 发送 HTTP 请求，服务端或 CDN 返回响应。
4. HTML 解析成 DOM；CSS 解析成 CSSOM。
5. 生成渲染树，布局，绘制，合成。
6. JS 可能阻塞解析；`defer` 保持顺序并在 DOM 解析后执行，`async` 下载完立即执行。

## 事件循环

- 一轮宏任务执行完，清空微任务队列，再进入渲染机会和下一轮任务。
- 常见微任务：`Promise.then`、`queueMicrotask`、MutationObserver 回调。
- 常见任务：定时器、网络事件、用户交互。
- `requestAnimationFrame` 在下一次绘制前执行，适合把高频 UI 更新按帧合并；它不等于“自动提升所有代码性能”。

## CORS

- 同源要求协议、主机、端口一致；限制的是浏览器脚本读取跨源响应。
- CORS 由服务端响应头授权，不是前端单方面解决。
- 非简单方法/头部/内容类型通常触发 `OPTIONS` 预检。
- 带凭证时，服务端不能用通配 Origin，并需允许 credentials；前端要显式开启凭证。
- 开发代理只改变请求发出位置，不是生产 CORS 配置。

## 强缓存与协商缓存

- 强缓存：`Cache-Control: max-age` 等，命中时不发网络请求。
- 协商缓存：发送 `If-None-Match` / `If-Modified-Since`；未变化返回 `304`。
- `ETag` 是资源版本标识，通常比秒级 `Last-Modified` 更精确；弱 ETag 表示语义等价。
- HTML 常用协商缓存；带 hash 的静态资源可用长期强缓存和 immutable。

## SourceMap

- 浏览器上报压缩后的 stack、release、文件和行列号。
- CI 保存产物与 SourceMap 的版本映射，服务端按 release 反解。
- 不把 SourceMap 公网暴露，可减少源码泄漏；chunk hash 与 release 必须严格匹配。

## 浏览器与网络方案取舍

### Map、Set 与排序去重

| 方案 | 适合场景 | 代价 |
| --- | --- | --- |
| `Map(key -> item)` | 对象数组按字段去重，需决定保留首项/末项 | 额外 `O(n)` 空间 |
| `Set` | 原始值去重，或只记录对象字段是否见过 | 仍需额外集合，不能直接表达冲突合并 |
| 排序后扫描 | 可修改顺序、希望降低额外索引结构 | `O(n log n)`，会改变原顺序或需要复制 |

面试结论：业务数组通常优先 Map/Set，时间 `O(n)`、语义清楚；只有内存特别敏感且允许改变顺序时再考虑排序扫描。

### 强缓存还是协商缓存

- 带内容 hash 的 JS/CSS：长期强缓存，因为 URL 变化就代表新内容。
- HTML/配置：更适合短缓存或协商缓存，避免长期拿到旧入口。
- 强缓存减少网络往返，但版本策略错误会导致旧资源难更新。
- 协商缓存仍有一次请求，适合内容是否变化需要服务端确认的资源。

### ETag 还是 Last-Modified

- `Last-Modified` 实现简单，但精度有限，内容变化后时间也可能不可靠。
- `ETag` 可按内容或版本生成，更精确，但服务端计算和分布式一致性有成本。
- 静态资源通常让构建/CDN提供版本；动态大文件是否使用内容 hash，要权衡计算成本。

### CORS、反向代理还是 BFF

| 方案 | 适用场景 | 代价 |
| --- | --- | --- |
| 服务端 CORS | 浏览器需要直接访问公开 API | 需要严格 Origin/凭证配置 |
| 同源反向代理 | 部署层可统一域名，接口较简单 | 多一层转发，环境配置要一致 |
| BFF | 需要隐藏密钥、聚合接口、统一鉴权和裁剪数据 | 多一跳与服务维护成本 |

不要把开发服务器 proxy 当成生产安全方案；生产仍要有明确的网关、CORS 或 BFF 边界。

### RAF、节流与防抖

- RAF：与绘制节奏对齐，适合滚动位置、动画、流式文本等 UI 更新。
- 节流：固定时间窗内最多执行一次，适合持续输入或滚动统计。
- 防抖：停止触发一段时间后执行，适合搜索建议、表单校验。
- RAF 控频到屏幕帧率，但不会自动减少单次计算量；重计算仍需分片或 Worker。

### 主线程分片还是 Web Worker

- 任务较短、需频繁访问 DOM：主线程分片或空闲调度，接入简单。
- 纯计算且持续时间长：Worker 避免阻塞交互，但有通信、序列化、取消和兼容成本。
- 先用 Performance trace 证明存在长任务，再决定是否引入 Worker。

### SourceMap 公网还是服务端解析

- 公网 SourceMap 调试方便，但可能暴露源码和目录结构。
- 服务端解析更安全，能统一版本、去重和权限；代价是要维护 release 与产物映射。
- 生产监控通常选择服务端解析，本地和测试环境保留便捷调试。

## 性能回答模板

> 用户说“页面慢”时，我不会先猜原因，而是先用指标把时间花在哪一层分开。TTFB 高或关键接口慢，优先看 CDN、网关、服务端和接口聚合；FCP/LCP 慢，就确认 LCP 元素是什么，再沿 Network waterfall 看 HTML、关键 CSS/JS、图片和接口依赖；TBT、Long Task 或 INP 差，结合 Performance flame chart 看 JS 解析执行、同步计算、React commit 和频繁状态更新；CLS 高则看图片尺寸、字体和异步内容回填造成的布局变化。
>
> 找到瓶颈后再选方案：网络侧做缓存、压缩、并行和资源优先级；JS 侧拆包、减少初始化和把纯重计算分片/移到 Worker；React 侧收敛状态更新、拆订阅边界并用 Profiler 验证；DOM 侧用虚拟列表或避免布局抖动。优化前后要在同一设备、同一数据和相同采样口径下对比相应指标，而不是只说“体感变快”。

### 高考落地页场景怎么接

> 这个项目不只关注“快不快”，还关注首屏稳定。若 TTFB、接口和资源时序正常，但 CLS/录屏仍有内容后补，我会转向 trace，检查首轮组件输入和后续 `UpdateComponentData` 是否不一致。发现数据回设后，处理的是数据读取时机和组件边界，而不是继续做网络优化。

### AI 流式页面场景怎么接

> 网络 chunk 到达快不代表 UI 流畅。如果每个 chunk 都 setState，会反复触发 React render、Markdown parse、commit 和滚动计算。这里用 buffer + `requestAnimationFrame` 合并同一帧内的 UI 更新，再用 Performance 的 Long Task 和 React Profiler 的 commit 次数/耗时验证；它优化的是渲染频率，不是模型吞吐。
