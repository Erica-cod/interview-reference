# AI 兴趣教练：二面产品故事与追问

> 适用场景：小鹏「AI 全栈产品研发培训生」二面，重点回答产品思考、用户反馈、方案取舍、Agent 与 GUI 边界、Multi-Agent 必要性及效果验证。
>
> 项目仓库：[Erica-cod/ByteDanceAiAgentProject](https://github.com/Erica-cod/ByteDanceAiAgentProject)

---

## 一、项目的核心产品故事

### 一句话定位

我独立开发了一个面向自主学习者的 AI 兴趣教练，尝试把用户模糊的学习目标，转换成有资料依据、可以保存、修改和执行的个性化计划。

### 一句话讲清迭代

我最初以为用户需要的是一个“能生成学习计划的 Agent”，但经过 4～5 位朋友的早期试用后，我发现真正的问题不是“能不能生成计划”，而是“用户能不能判断计划是否适合自己，并真正开始执行”。因此，后续改造重点不只是增加 Multi-Agent，而是把产品升级为：

> 对话理解目标 + 结构化约束确认 + 计划生成与审核 + 可编辑 Plan Card + 执行反馈

### 一句话讲清产品原则

> Agent 负责模糊目标、跨工具搜索和动态重规划；GUI 负责信息确认、局部编辑、任务执行和状态反馈；确定性代码负责日期、时长、依赖等规则校验。

---

## 二、2～3 分钟完整口述版

我独立做过一个面向自主学习者的 AI 兴趣教练。用户可以输入一个比较开放的学习目标，例如“帮我制定一个从下周开始、持续三个月的乒乓球入门计划”。系统会联网搜索相关资料、展示信息来源、生成学习计划，并把计划保存成可以后续修改的任务。

第一版 MVP 的核心假设是：相比普通聊天机器人，如果系统能够联网搜索、展示信息来源，并把回答转换成结构化计划，用户会更信任结果，也更容易执行。

做完第一版以后，我让身边大约 4～5 位朋友在线下单机环境中试用。这不是一次正式的统计实验，更像是早期探索性可用性测试。当时比较一致的正向反馈是：加入搜索和来源后，回答看起来更有依据，用户对结果的信任感有所提升。但负向反馈是：计划虽然方向基本合理，却不够具体，用户看完后仍然不知道今天应该做什么、做到什么程度算完成，也很难判断三个月的安排是否真的适合自己。

我后来复盘发现，这不是简单地让模型“写得更长”就能解决的问题，而是至少有四层原因。

第一，输入约束不足。用户只说了想学乒乓球，但没有说明当前基础、具体目标、每周可投入时间、是否有场地和教练等信息；而且用户自己可能都不知道制定计划需要提供哪些条件。

第二，计划的数据结构太粗。当时任务主要只有标题、预计时长、截止时间和标签，缺少阶段目标、前置依赖、学习资源、完成标准和风险，因此模型即使填满了字段，也不代表计划真正可执行。

第三，单个 Agent 同时负责生成和检查自己的计划，容易出现自我确认。计划可能在格式上完整，但在训练强度、时间分配和阶段衔接上并不合理。

第四，纯聊天产品缺少确认和执行闭环。用户拿到一大段答案后，很难局部编辑，也不能清楚分辨哪些信息是自己提供的、哪些是系统推测的。

所以后续我的改进并不是简单地把单 Agent 一刀切成 Multi-Agent，而是重新设计整个产品流程。

用户先通过对话表达目标，系统提取已有信息，然后通过结构化卡片补充几个高信息量的约束，例如当前基础、具体目标、可投入时间、资源条件和身体限制。接下来由 Planner 生成分阶段计划，Critic 从可行性、现实性、完整性和风险角度提出问题，Host 控制修改和验证流程，Reporter 再把内部结果压缩成用户能够理解的版本。日期、总时长和任务依赖这类确定性问题则交给代码校验，而不是全部依赖模型判断。

最终结果也不只作为一段聊天文本展示，而是进入可编辑的 Plan Card。用户可以确认或修改每周频次、任务时间、所需资源和完成标准。日常的勾选、编辑和查看进度继续使用 GUI；只有遇到“这周只完成了一次，帮我重新安排”这类模糊、动态的问题时，才再次调用 Agent。

从工程上，我已经实现了联网搜索、来源展示、计划持久化、计划工具和 Multi-Agent 编排。但当时没有再做一轮严格的同任务对照测试，所以我不会直接声称 Multi-Agent 已经提升了用户满意度。下一步我会用计划确认率、首次任务启动率、用户修改字段、7 日任务完成率，以及延迟和 Token 成本来验证它是否真正产生了产品价值。

这个项目给我最大的产品启发是：Agent 降低了传统软件的操作门槛，但也可能把门槛转移到目标表达和结果判断上。好的 Agent 产品不是把所有 GUI 都替换掉，而是让 Agent 处理模糊目标和动态规划，让 GUI 承担确定性的确认、编辑和执行。

---

## 三、90 秒口述版

我独立做过一个面向自主学习者的 AI 兴趣教练。用户输入开放的学习目标后，系统可以联网搜索、展示来源、生成计划并保存成任务。

第一版 MVP 完成后，我让大约 4～5 位朋友做了早期试用。正向反馈是，加入搜索和来源后，回答显得更有依据，信任感有所提升；但他们也反馈计划不够具体，虽然看起来方向合理，却不知道每天应该做什么、做到什么程度算完成，也很难判断计划是否适合自己。

我复盘后发现问题至少有三层：第一，用户没有提供基础、目标、每周时间和资源等关键约束，而且用户自己可能不知道应该提供什么；第二，原来的计划结构太粗，缺少阶段、依赖、完成标准和风险；第三，单个 Agent 既生成又自检，格式完整不代表语义上可行。

因此后续我没有只靠拉长 Prompt，而是重新设计了产品流程：先用对话理解目标，再用结构化卡片补充关键约束；由 Planner 生成计划，Critic 检查可行性和风险，Host 控制修改与验证，Reporter 负责面向用户表达；最终结果进入可编辑的 Plan Card，由用户确认后再执行。日常查看、编辑和打卡使用 GUI，动态调整才调用 Agent。

Multi-Agent 的目的不是让内容更长，而是把生成、审查、修改和验证拆成可观察的过程。不过我当时没有完成正式的二次用户测试，所以工程实现已经完成，产品效果仍需通过计划确认率、首次任务启动率和 7 日完成率进一步验证。

---

## 四、记忆框架：八步讲完整故事

不要逐字死背，记住以下八个关键词即可：

> 场景 → 假设 → MVP → 试用 → 反馈 → 诊断 → 改造 → 验证

| 环节 | 本项目内容 |
|---|---|
| 场景 | 自主学习者目标模糊，难以形成适合自己的执行计划 |
| 假设 | 搜索依据 + 结构化计划可以提升信任和可执行性 |
| MVP | 搜索、来源展示、计划生成、计划保存和后续修改 |
| 试用 | 约 4～5 位朋友，线下单机、探索性可用性测试 |
| 反馈 | 回答更有依据，但计划不够具体、难以判断是否可行 |
| 诊断 | 输入约束缺失、计划结构太粗、单 Agent 自我审核、缺少执行闭环 |
| 改造 | 约束卡片 + Plan Schema + Multi-Agent 审核 + 规则校验 + 可编辑 GUI |
| 验证 | 工程能力已实现；产品增益仍需用采纳、启动、完成和成本指标验证 |

---

## 五、用户反馈应该如何具体表达

不要只说“计划不够详细”。应把它翻译成可以被设计和验证的产品问题：

- 不知道今天具体做什么；
- 不知道一次练习需要多长时间；
- 不知道做到什么程度才算完成；
- 不清楚完成任务需要哪些资源；
- 不知道一个阶段与下一个阶段如何衔接；
- 没完成任务后，不知道后续计划如何调整；
- 无法判断整个安排是否符合自己的基础、时间和资源条件。

### 乒乓球计划示例

粗粒度计划：

> 第一周学习正手攻球。

更可执行的计划至少应该明确：

- 每周练习几次、每次多长时间；
- 是徒手挥拍、墙练、陪练还是多球训练；
- 每次分别完成多少组；
- 需要什么场地、器材或陪练资源；
- 达到什么动作稳定性或连续成功率后进入下一阶段；
- 如果当周只完成一次，后续如何调整。

因此，用户需要的不是更长的文本，而是颗粒度合适、带完成标准、可修改、可反馈的行动单元。

---

## 六、从反馈到方案的推导

| 用户阻塞 | 产品诊断 | 对应方案 |
|---|---|---|
| 计划不适合自己 | 输入约束缺失 | 对话提取信息 + 约束确认卡片 |
| 不知道今天做什么 | 任务结构太粗 | 阶段、任务、时长、资源、完成标准 |
| 不知道计划是否合理 | 缺少独立语义审核 | Planner + Critic；必要时引入外部依据 |
| 日期或总时长矛盾 | 确定性规则缺失 | 使用代码校验日期、时长和依赖 |
| 一大段文本难修改 | 纯对话缺少可控性 | 可编辑 Plan Card |
| 执行偏离后无法处理 | 缺少反馈闭环 | 完成记录 + 局部重规划 |

关键表达：

> Multi-Agent 只解决了其中一部分语义审核问题，不是整个产品方案。

---

## 七、Agent、GUI 和确定性代码的边界

### 适合 Agent 的任务

- 理解用户模糊、开放的目标；
- 识别缺少的关键约束；
- 跨搜索、记忆、计划等工具完成编排；
- 根据多个约束生成候选计划；
- 当现实情况变化时动态重规划。

### 适合 GUI 的任务

- 展示和确认已提取的约束；
- 修改某一个字段或任务；
- 对计划进行局部编辑；
- 勾选任务、查看进度和状态；
- 对高频、确定性的操作提供稳定入口。

### 适合确定性代码的任务

- 日期是否合法、是否冲突；
- 总时长是否超过用户可投入时间；
- 截止时间是否早于开始时间；
- 前置任务是否已经完成；
- 数据结构、字段类型和必填项是否正确。

### 面试总结句

> Agent 和 GUI 不是二选一。Agent 处理不确定性，GUI 提供可见性和控制感，代码规则保证确定性边界。

---

## 八、Multi-Agent 设计与必要性

### 四个角色

| 角色 | 主要职责 | 产品价值 |
|---|---|---|
| Planner | 把目标拆成阶段、任务、时间和完成标准 | 负责构建候选方案 |
| Critic | 检查可行性、现实性、完整性和风险 | 降低遗漏和自我确认 |
| Host | 传递未解决问题、控制修改与最大轮次 | 防止讨论失控和成本无限增长 |
| Reporter | 压缩内部过程，生成用户可理解的结果 | 控制信息负担，提升可读性 |

其中 Planner 和 Critic 是核心内容角色；Host 更像工作流控制器，Reporter 更像用户体验层。后两者并不一定必须由独立大模型承担，也可以逐步用工作流或确定性代码替代。

### 什么时候需要 Multi-Agent

- 时间跨度长；
- 目标和约束较多；
- 阶段之间存在依赖；
- 计划失败的代价较高；
- 需要从不同目标出发进行独立审核。

### 什么时候不需要 Multi-Agent

- 简单知识问答；
- 约束很少的短期计划；
- Prompt + Schema + 规则校验已经足够；
- Multi-Agent 增加延迟和成本，却没有改善用户行为。

### 方案升级顺序

> 补输入约束 → 补 Plan Schema → 增加确定性校验 → 单 Agent 自检 → 复杂任务启用 Multi-Agent

### 两句关键表达

> Schema 解决“有没有写”，Critic 解决“写得合不合理”。

> Multi-Agent 的目的不是让回答更长，而是把生成、审查、修改和验证拆成可观察、可测试的过程。

---

## 九、最可能的面试追问与回答

### 1. 为什么做这个产品？

自主学习者并不缺少知识内容，真正困难的是把模糊目标转化成符合个人时间和资源约束的执行计划，并在执行中持续调整。因此我希望验证 Agent 能否把“信息获取—计划生成—任务执行—动态调整”串成闭环，而不只是再做一个聊天机器人。

### 2. 4～5 个朋友的反馈有代表性吗？

没有统计代表性。我把它定义为早期探索性可用性测试，而不是正式用户研究。它的作用不是证明市场需求，而是尽早发现最明显的使用障碍。多位用户出现了相似问题，因此我把它转化成一个需要进一步验证的假设，而不是直接得出“所有用户都需要 Multi-Agent”的结论。

### 3. 用户说“不够详细”，究竟是什么意思？

我把它继续拆解为：不知道今天做什么、不知道投入多长时间、不知道做到什么程度算完成、不清楚需要什么资源、无法判断是否适合自己的时间和基础。因此，“详细”不是字数问题，而是信息能否支持用户采取下一步行动。

### 4. 你怎么从这条反馈推导到改进方案？

我没有直接把用户原话翻译成“增加文字”，而是分析阻塞发生在哪一步：输入不完整、输出结构不支持执行、计划语义上不可行，还是缺少确认和反馈。然后分别用约束卡片、Plan Schema、语义审核、规则校验和执行闭环解决。Multi-Agent 只是语义审核层的一个方案。

### 5. 为什么不直接把 Prompt 写得更详细？

我会先改 Prompt 和 Schema，因为成本最低。但 Schema 只能约束字段是否存在，无法保证字段组合是否合理。例如计划格式完整，但用户每周只有一小时，目标却需要高强度训练，这属于语义上的不合理。因此简单计划用 Prompt、Schema 和规则即可，复杂计划才增加独立语义审核。

### 6. 单 Agent 自我反思不可以吗？

可以，而且应当作为更低成本的基线方案。Multi-Agent 主要用于约束复杂、时间跨度长的计划，通过给 Planner 和 Critic 不同目标，降低同一上下文中的自我确认，并把生成、质疑、修改、验证拆成可记录的步骤。但它会增加延迟和 Token 成本，因此不应默认用于所有请求。

### 7. Multi-Agent 能保证计划可行吗？

不能。Critic 仍然是概率模型，只能提高审核覆盖率，不能成为权威认证。计划是否可行还取决于用户约束是否真实、外部依据是否可靠，以及执行后的真实反馈。因此系统生成的是候选计划，需要展示假设和风险，由用户确认，并在执行过程中校准。

### 8. 用户自己不知道计划是否可行，怎么让他确认？

不能直接问用户“你觉得计划是否可行”，而应把抽象判断转换成用户能回答的具体事实。例如“你每周是否能稳定练习三次、每次 60 分钟”“是否有球台和陪练”。同时区分三类信息：用户明确提供、系统推测、尚待确认。用户负责确认自己的真实约束，系统负责专业推理和风险提示。

### 9. 这是不是把 GUI 一刀切改成了 Agent？

第一版确实存在这个问题。自然语言降低了寻找入口和填写表单的门槛，却把门槛转移到了目标表达和结果判断上。重新设计后，我会让 Agent 负责理解模糊目标和动态调整，让 GUI 负责约束确认、计划编辑、任务打卡和状态展示，让代码负责确定性校验。

### 10. 为什么需要结构化卡片，不能都在对话里完成吗？

纯对话自然，但用户看不到当前信息是否完整，也不方便只修改某一个字段。卡片可以集中展示系统理解的目标、基础、时间和资源条件，让用户补充缺失项并确认推测项。理想流程不是让用户从零填写长表单，而是系统先从对话中自动填写，用户只负责检查和补充。

### 11. 会问用户多少问题？一直追问会不会很烦？

不能为了信息完整无限追问。我会优先询问三到四个对计划影响最大的变量，例如目标、当前基础、每周可投入时间和资源条件。影响较小的信息可以使用默认值，但必须标记为系统假设。同时关注澄清阶段退出率，避免前置成本过高。

### 12. 如何定义一份好计划？

我会从五个维度判断：是否使用了用户约束，任务是否有时间、动作和完成标准，总投入是否现实，阶段之间是否合理递进，以及用户未完成时能否局部调整。前三项可以静态审核，后两项更需要真实执行数据验证。

### 13. Multi-Agent 上线后提升了多少？

目前不能声称有明确提升。Multi-Agent 工程版本已经完成，但没有做正式的第二轮同任务对照测试。当前能够证明的是工作流已经实现，能否改善用户采纳和执行仍需实验。如果补做，我会比较相同 Plan Schema 下的单 Agent 和 Multi-Agent，观察计划确认率、修改字段、首次任务启动率，同时记录延迟和 Token 成本。

### 14. 怎么验证它不是只让答案变长？

让单 Agent 和 Multi-Agent 输出同一套 Plan Schema，并控制最终展示长度。离线比较约束覆盖、时间冲突、阶段依赖和风险识别；在线比较计划采纳、用户修改、首次任务启动和 7 日完成。如果只提高离线评分，却没有改善用户行为，就不应默认启用。

### 15. 如何控制延迟和成本？

根据任务复杂度进行路由：知识问答用单 Agent；简单计划使用单 Agent、Schema 和规则校验；复杂长周期计划才启用 Planner 和 Critic。同时限制最大讨论轮数，只让 Planner 修改尚未解决的问题。产品体验上也可以先展示草案，再异步完成更深的审核。

### 16. Planner 和 Critic 一直无法达成一致怎么办？

Host 不应让它们无限讨论，而应检查 Critic 的关键问题是否已经被响应，并设置最大轮次。无法自动解决的分歧不应强行形成一致，而应转换成用户能够理解的选项，例如“进度更快但强度更高”与“进度较慢但更稳妥”，交给用户根据偏好选择。

### 17. 项目中你具体负责什么？

项目由我独立完成。我负责需求定义、产品流程设计、前后端实现、Agent 工具设计、搜索和数据库接入、计划持久化以及 Multi-Agent 编排。早期试用也是我自己组织的。不过当时用户研究和埋点体系并不完整，因此我会明确区分工程事实、少量用户反馈和待验证的产品假设。

### 18. 你当时最大的错误是什么？

我过早地把“生成一份计划”当成了产品目标，更多关注模型能否搜索、调用工具和输出结构化数据，却忽略了用户真正关心的是计划是否适合自己、是否能理解和修改，以及最终有没有开始执行。另外，我把自然语言等同于低门槛，却忽略了用户可能不知道应该提供哪些约束。

### 19. 如果重新做一次，你会怎么安排优先级？

我会先用单 Agent、约束卡片和更细的 Plan Schema 做一个低成本版本，验证哪些因素真正影响用户采纳和启动。然后补规则校验，只有在复杂计划仍然频繁出现语义不合理时，才引入 Multi-Agent。最后补执行数据和动态重规划，形成真正的闭环。

---

## 十、压力追问

### “听起来根本不需要 Multi-Agent。”

如果目标只是生成格式完整的计划，我同意，确实不需要 Multi-Agent，Prompt、Schema 和规则校验可能已经足够。Multi-Agent 的前提是任务存在多个约束，并且需要从不同目标进行语义审核。即便如此，也必须通过实验验证相对于单 Agent 的增益；如果没有改善用户行为，就应降级到更简单的方案。

### “用户不会判断计划，让他确认有什么意义？”

用户未必能判断专业计划整体是否正确，但能确认自己的真实约束。他可能不知道三个月的乒乓球计划是否专业，却知道自己一周只能练两次、没有发球机、无法请教练或者膝盖有旧伤。系统负责专业推理，用户负责确认自己能够判断的事实，无法确定的风险则需要透明展示。

### “四五个人随口反馈，你就重构产品，是不是太草率？”

如果直接全面重构，确实草率。更合理的做法是先把反馈转化成假设，再用低成本原型验证。例如先增加约束卡片和 Plan Schema，观察用户的修改行为和首次任务启动；只有复杂计划仍然存在明显的语义问题时，再启用 Multi-Agent。此前的实现更偏技术探索，这是我现在从产品角度复盘后会调整的地方。

### “你怎么知道用户要的是更细，而不是更短？”

不能仅根据“不够详细”四个字得出结论，而要观察用户停在哪一步并继续追问。如果用户说内容太多、不知道重点，需要的是信息分层；如果不知道今天练什么，需要的是任务颗粒度；如果每周根本没有这么多时间，是约束不匹配；如果不知道安排是否专业，是信任和审核问题。所以详细度不是字数问题，而是信息是否支持下一步行动。

---

## 十一、产品指标设计

北极星指标不应该是“生成了多少份计划”，因为生成并不等于价值实现。更合理的价值信号是：

> 用户确认了一份适合自己的计划，并真正开始和持续执行。

| 阶段 | 指标 | 目的 |
|---|---|---|
| 需求澄清 | 关键约束完成率 | 判断系统是否获得足够信息 |
| 澄清体验 | 澄清阶段退出率 | 判断前置提问是否过多 |
| 生成性能 | 生成成功率、P95 延迟 | 判断系统是否稳定、可接受 |
| 计划采纳 | 计划确认率 | 判断用户是否愿意采用计划 |
| 用户修改 | 修改率、修改字段及原因 | 定位最不符合用户预期的部分 |
| 开始执行 | 首个任务启动率 | 判断计划是否真正促成行动 |
| 持续执行 | 7 日任务完成率 | 判断计划是否现实、可持续 |
| 动态调整 | 重规划率及原因 | 判断是环境变化还是初始计划质量差 |
| 成本质量 | Token、延迟、失败率 | 判断 Multi-Agent 的增益是否值得 |

MVP 阶段优先观察三个指标：

1. 计划确认率；
2. 首个任务启动率；
3. 7 日任务完成率。

---

## 十二、事实边界

### 可以明确说

- 第一版单 Agent MVP 已经实际完成；
- 实现了搜索、来源展示、计划生成和保存；
- 确实给大约 4～5 位朋友进行过线下单机试用；
- 用户认为加入搜索和来源后，回答显得更有依据；
- 用户反馈计划不够具体、不方便直接执行；
- 后续确实实现了计划工具、持久化和 Multi-Agent 编排；
- 现在复盘后，认识到纯 Agent 可能增加目标表达和结果判断门槛。

### 不能说

- 搜索彻底解决了幻觉；
- 4～5 位朋友证明了市场需求；
- 做过正式的大规模用户研究；
- 做过 A/B 实验；
- Multi-Agent 已经显著提升满意度或完成率；
- 所有用户都需要更详细的计划。

### 推荐措辞

说：

> 用户主观上认为加入来源后的回答更有依据，信任感有所提升。

不要说：

> 加入搜索以后，回答已经没有幻觉了。

说：

> Multi-Agent 的工程实现已经完成，产品效果仍需进一步验证。

不要说：

> Multi-Agent 明显提高了用户满意度。

---

## 十三、联系一面问题的加分表达

如果面试官再次问 GUI 改造成 Agent 的门槛，可以主动联系本项目：

> 一面您问到图书管理系统从 GUI 一刀切改成 Agent 的门槛。面试后我重新复盘了自己的兴趣教练，发现第一版也有类似的问题：自然语言入口虽然降低了寻找功能和填写表单的门槛，却把门槛转移到了目标表达和结果判断上。用户可能不知道应该提供哪些约束，也未必有能力判断模型生成的计划是否可行。
>
> 如果重新设计，我会采用混合形态：用户先通过对话表达开放目标，系统自动提取信息并用卡片让用户补充关键约束；内部再根据任务复杂度选择单 Agent 或 Multi-Agent；最终计划通过可编辑 GUI 让用户确认和执行。Agent 负责不确定性，GUI 负责可见性和控制感。

这段回答可以体现：

- 能吸收一面的反馈；
- 会反思自己的产品；
- 不把 Agent 当成目的；
- 理解 AI 产品的能力边界与交互代价。

---

## 十四、面试前一分钟速记

### 核心矛盾

> 从“能生成”升级到“能判断、能确认、能执行”。

### 四层问题

1. 用户不知道该提供哪些约束；
2. 计划结构过粗，无法直接执行；
3. 单 Agent 自我审核，格式完整不等于语义合理；
4. 纯聊天缺少编辑、确认和执行反馈。

### 五件改造

1. 对话提取目标；
2. 结构化约束卡片；
3. Plan Schema 和规则校验；
4. 复杂任务进行 Multi-Agent 审核；
5. 可编辑 Plan Card 和执行闭环。

### 三条边界

1. 4～5 人只是探索性测试，不是统计结论；
2. 搜索提高用户对依据的感知，不等于消灭幻觉；
3. Multi-Agent 已实现，但产品增益尚未经过正式对照验证。

### 三句必须记住的话

> Schema 解决“有没有写”，Critic 解决“写得合不合理”。

> Multi-Agent 不是让内容更长，而是让生成、审查、修改和验证变得可观察、可测试。

> Agent 处理不确定性，GUI 提供可见性和控制感，确定性代码守住规则边界。

---

## 十五、最终收尾句

> 这个项目让我认识到，AI 产品的目标不是尽可能多地使用 Agent，而是找到用户任务中真正不确定、值得交给模型处理的部分。计划生成只是能力展示，只有当用户能够理解、确认并持续执行时，产品价值才真正发生。
