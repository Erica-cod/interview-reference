# Text2SQL 私域问数项目——面试介绍说辞

## 一、完整项目介绍（可直接口述）

这个项目是一个面向研究所内部业务的 Text-to-SQL 智能问数系统。项目最核心的约束不是单纯追求模型能力，而是数据合规和保密：数据库中的表结构、业务字段、关联关系以及实际查询结果都属于内部数据，不能直接发送给 GPT、Claude 等公网模型。因此，我们采用内网私有化部署方案，将千问系列模型部署在本地 GPU 服务器上，整个 Schema 检索、查询规划、SQL 生成和数据库执行链路都在私域环境中完成。Qwen3-32B-Thinking属于稠密模型，推理能力相对稳定，在复杂意图理解、推理链生成以及结构化任务输出方面表现较好，因此主要用于对推理质量要求较高的场景。Qwen3-30B-A3B-Thinking属于MoE模型，在保持较强推理能力的同时，推理成本和部署成本相对更低，能够满足部分在线场景下的性能需求。显卡资源是8张RTX 4090，单卡24GB显存，总显存约192GB。模型选型早期部署Qwen2.5-72B，后续替换为Qwen3-32B-Thinking。新模型参数量更小，但推理能力更强，响应速度和资源利用率也更适合问数项目。
业务接入： 上层系统通过统一HTTP接口调用模型，模型替换时只需要修改模型路径和服务配置，不需要调整整体业务流程。

用户输入一个自然语言问题后，第一步不是立即生成 SQL，而是先进行意图理解和业务检索词抽取。这里使用一个相对轻量的模型，将用户问题拆成适合 Schema 检索的业务级关键词。例如用户输入“查询总交易笔数大于 50000 的利率是多少”，模型会抽取出“总交易笔数”和“利率”。这里的小模型负责的是业务语义抽取，不是完成所有底层分词。

进入 BM25 召回后，系统还会进行一次确定性的中英文分词。英文、数字和下划线按照连续单词处理；中文会保留完整词、单字以及相邻二元词。例如“销售额”会生成“销售额”“销”“售”“额”“销售”“售额”。这些二元词是相邻字符形成的 bigram，不是把任意两个字进行全排列组合。这样做是为了提高中文字段名、字段别名和业务描述的字面召回率。

Schema 检索采用 BM25 和 Embedding 两路召回。BM25 属于稀疏字面召回，比较擅长命中字段名、表名、别名和业务描述中直接出现的词；Embedding 属于稠密语义召回，即使用户表达和数据库字段描述不完全一致，也可以召回语义相近的候选字段。每一个业务检索词都会分别经过 BM25 和 Embedding 得到候选排名。

由于两种召回方式的原始分数不在同一个量纲上，我们没有直接把 BM25 分数和向量相似度相加，而是采用 RRF，也就是倒数排名融合，根据候选字段在不同召回通道中的名次进行融合。一个字段如果同时被 BM25 和 Embedding 排在前面，就会得到更高的融合分数。这样既保留字面匹配能力，也能利用语义召回补充同义表达。

RRF 的目标是扩大召回覆盖率，因此得到的仍然是一批粗排候选。接下来我们使用 Rerank 模型进行精排。Rerank 会把用户的原始问题和每个候选字段的完整 Schema 文档成对输入模型。Schema 文档不只包含物理字段名，还包含数据库名、表名、字段类型、字段含义、业务别名、语义角色、样例或取值范围、业务用途等信息。Rerank 模型根据 Query 和候选文档的整体语义相关性重新打分，把真正支撑当前查询的字段排到前面。

### BM25、Embedding、RRF 和 Rerank 的实际案例

为了验证这一段链路，我们使用项目自带的交易数据库和检索实现做过一次离线回放。测试问题是：

```text
查询总交易笔数大于50000的利率是多少
```

轻量关键词抽取模块首先得到：

```json
["总交易笔数", "利率"]
```

这个例子的目标字段实际上有两个：`trade_summary.total_trade_count` 是筛选字段，`interest_info.interest_rate` 是最终输出字段。两张表还需要通过 `user_id` 进行关联。

其中，`total_trade_count` 的 Rerank 文档包含：

```text
字段：trade_summary.total_trade_count
含义：用户累计交易总笔数
别名：总交易笔数、累计交易笔数、交易笔数、交易次数
取值范围：整数，>=0
数据分布：高频交易用户可能超过50000
用途：筛选总交易笔数大于某个阈值的用户
```

`interest_rate` 的 Rerank 文档包含：

```text
字段：interest_info.interest_rate
含义：用户对应年化利率
别名：利率、年化利率、用户利率、利率数值
取值范围：0-100
用途：当用户询问利率是多少时，通常作为输出字段
```

为了形成对照，数据库中还存在两个容易混淆的字段：

```text
trade_summary.active_days：用户活跃交易天数
interest_info.rate_type：利率类型，例如 standard、vip、high_value
```

面试时可以把这个案例压缩成下面这段话：

> 例如用户问“查询总交易笔数大于 50000 的利率是多少”，关键词模块会抽取“总交易笔数”和“利率”。BM25 对“利率”召回时，`interest_rate` 得分 6.43，`rate_type` 得分 5.81，两者非常接近；本地向量召回甚至把 `rate_type` 以 0.53 排在第一，而真正的 `interest_rate` 只有 0.23。经过 RRF 融合后两个字段都会被保留下来，再由 Rerank 阅读完整 Query 和字段业务描述。它看到用户问的是“利率是多少”，而 `interest_rate` 的用途是输出利率数值，`rate_type` 只是 standard、vip 等分类，因此把 `interest_rate` 排到 `rate_type` 前面。最后再自动补充两张表的 `user_id` 关联键，构造成后续 Plan Agent 使用的 SchemaGraph。这个案例能够说明 BM25 和 Embedding 负责尽量召回，RRF 负责融合，Rerank 才负责在相似字段中做细粒度语义判别。

精排完成后，系统不会把整个数据库 Schema 交给后续模型，而是把选中的字段、字段所属表以及相关的主外键关系构造成一个局部 SchemaGraph。SchemaGraph 可以理解成当前问题所需要的数据库子图，它主要包含数据库、相关表、相关字段以及表之间的关联关系。

然后由 CoT Plan Agent 读取用户问题和这个经过裁剪的 SchemaGraph，生成结构化查询计划。项目中的计划不是让模型输出一段不可控的自由思维过程，而是要求它输出显式四元组：数据库、处理对象、操作指令和输出目标。

例如前面的查询可以规划为：数据库是 `trade_db`；处理对象包括 `trade_summary.total_trade_count`、两张表中的 `user_id`、`interest_info.interest_rate` 以及 `user_id` 关联关系；操作指令是先筛选总交易笔数大于 50000 的用户，再基于 `user_id` 关联利率表，最后取得利率；输出目标是 `interest_info.interest_rate`。Plan Agent 只负责决定查什么以及按照什么顺序查，不直接编写 SQL。

在 SQL 生成阶段，我们还会根据当前四元组从 SchemaGraph 中提取一次更小的 LocalSchema。也就是说，Schema 裁剪一共分为两层：第一层通过检索从全量数据库 Schema 得到 Query 相关的 SchemaGraph；第二层根据当前执行步骤，从 SchemaGraph 中得到 Coder Agent 真正需要的 LocalSchema。这样可以减少上下文长度、无关字段干扰以及表字段幻觉。

接下来由 Coder Agent 读取四元组、LocalSchema、数据库要求以及可能存在的修正反馈，将结构化计划转换成 SQL。通过将 Plan 和 Coder 解耦，我们可以更清楚地区分错误到底来自 Schema 召回、执行计划，还是 SQL 翻译阶段，也方便针对单个模块进行评测和回调。

SQL 生成后，会转换成包含数据库名称和 SQL 的结构化执行请求，通过统一数据库工具路由层发送到对应数据源。项目中将这一层称为 MCP Router，它负责根据数据库名称选择对应执行器。数据库侧使用只读连接，并限制只允许执行 `SELECT` 或 `WITH` 查询，避免模型执行更新、删除和建表等高风险操作。

SQL 能够成功运行，只能说明语法和执行环境没有问题，不能直接证明业务语义正确。因此，执行完成后，系统还会汇总 Schema 检索结果、CoT 四元组、LocalSchema、生成的 SQL、数据库执行状态和结果集，交给最终校验模块判断整个链路是否真正满足用户原始问题。

如果校验成功，系统会把数据库执行结果整理成用户可以理解的自然语言结果并返回前端。如果校验失败，系统会优先定位最早的根因阶段：Schema 缺失或召回错误就从 Schema 检索重新执行；Schema 正确但四元组规划错误就回调 Plan Agent；计划正确但 SQL 的字段、Join、筛选或聚合错误就回调 Coder Agent；SQL 本身合理但出现数据库路由、连接、权限或超时问题，则只重新执行 SQL。错误原因会作为修正反馈传回对应模块，达到最大重试次数后再向前端返回明确的失败阶段和原因。

因此，整个链路可以概括为：

```text
用户 Query
→ 业务检索词抽取
→ BM25 确定性分词
→ BM25 稀疏召回 + Embedding 语义召回
→ RRF 排名融合
→ Rerank 语义精排
→ 构造局部 SchemaGraph
→ Plan Agent 生成显式四元组
→ 根据四元组提取 LocalSchema
→ Coder Agent 生成 SQL
→ 数据库工具路由并只读执行
→ 全链路结果校验
→ 成功返回，失败按根因阶段定向回调
```

## 二、推理模型选型（可直接口述）

项目中的 CoT 规划主要使用 Qwen 系列推理模型，包括 Qwen3-32B-Thinking 和 Qwen3-30B-A3B-Thinking。

Qwen3-32B-Thinking 属于稠密模型，推理表现相对稳定，在复杂意图理解、多步查询规划以及结构化四元组输出方面表现较好，因此主要用于对推理质量和稳定性要求更高的场景。

Qwen3-30B-A3B-Thinking 属于 MoE 模型，总参数规模较大，但单次推理只激活其中一部分参数。在保持较强推理能力的同时，它的实际计算成本相对更低，因此更适合部分对并发量和响应时延要求较高的在线场景。

选择 Qwen 系列的首要原因是私域部署要求。业务 Schema 和查询数据不能发送到公网模型服务，所以 GPT、Claude 等外部旗舰模型只能在使用公开数据、脱敏 Schema 和合成数据的离线评测中作为能力对照，不能直接进入生产链路。在可私有化部署的模型中，Qwen 系列在中文业务理解、复杂推理、结构化输出和工具调用方面比较均衡，也比较适合当前的 Text-to-SQL 场景。

目前我们也在持续评估更大规模模型，包括 Qwen3-235B-A22B-Thinking 和 DeepSeek 系列模型，重点观察它们在复杂问数、多步推理、长 Schema 理解以及跨库分析场景中的规划准确率和输出稳定性。不过模型是否升级不能只看公开 Benchmark，还需要综合比较 Schema 命中率、四元组正确率、SQL 执行准确率、首 Token 时延、整体吞吐和部署成本。

## 三、模型部署与硬件资源（可直接口述）

项目面向研究所内部使用，模型部署在完全隔离的内网环境中。推理服务使用 vLLM，服务器配置为 8 张 RTX 4090，每张显卡 24GB 显存，总显存约 192GB。

早期我们部署的是 Qwen2.5-72B，后续切换到 Qwen3-32B-Thinking。Qwen3-32B 的参数规模更小，但在推理和结构化任务上的能力更适合当前问数场景，因此在响应速度、资源占用和推理质量之间取得了更好的平衡。

部署时，首先将经过内部审核的模型权重下载到内网模型仓库，在目标服务器上准备 CUDA、PyTorch 和 vLLM 环境。启动 vLLM 时通过 Tensor Parallel 将模型权重切分到 8 张 GPU 上，并根据模型大小和业务压力配置数据类型、最大上下文长度、GPU 显存利用率、最大并发序列数和批处理 Token 数量。

由于 RTX 4090 单卡只有 24GB 显存，而且多卡之间主要通过 PCIe 通信，没有数据中心显卡常见的高速 NVLink 互联，所以部署时不仅要看模型权重能否装下，还要关注 KV Cache 占用、跨卡通信开销、最大上下文长度以及高并发情况下是否发生显存溢出。我们会根据实际压测结果调整 `tensor_parallel_size`、`max_model_len`、`max_num_seqs` 和 `gpu_memory_utilization` 等参数，在吞吐、延迟和稳定性之间做平衡。

vLLM 对外提供 OpenAI Compatible API，上层 Text-to-SQL 系统统一通过 HTTP 接口调用模型服务。业务代码只依赖统一的 `base_url`、模型名称和鉴权配置，不直接依赖某一个具体模型的内部实现。这样在 Qwen2.5-72B、Qwen3-32B-Thinking 或其他模型之间切换时，主要修改模型路径和服务配置，不需要重新调整 Schema 检索、Plan、Coder 和校验等整体业务流程。

模型服务启动后，我们会先执行健康检查和预热请求，再进行并发压测，重点记录首 Token 时延、完整响应时延、每秒输出 Token 数、并发吞吐、GPU 显存占用、GPU 利用率、请求队列长度和失败率。生产环境还会限制最大输入长度、最大输出长度、并发数和请求超时时间，避免超长 Schema 或异常请求拖垮整个推理服务。

整个部署与调用关系可以概括为：

```text
内网模型权重仓库
→ 8×RTX 4090 推理服务器
→ vLLM 多卡 Tensor Parallel
→ OpenAI Compatible HTTP API
→ Text-to-SQL 统一模型客户端
→ Keyword / Plan / Coder / Validation 等业务模块
```

模型服务和业务逻辑解耦后，一方面能够满足研究所的数据合规和内网部署要求；另一方面也方便我们持续替换和评估新的开源模型，而不需要重写上层问数链路。
