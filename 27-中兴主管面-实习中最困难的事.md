# 中兴主管面：实习中最困难的一件事

> 推荐故事：字节大模型 Markdown 渲染质量治理
>
> 核心能力：面对模糊问题定义边界、技术权衡、跨角色对齐、主动推进、结果负责。
>
> 表达要求：先说“为什么难”，再讲自己的两个关键动作；普通回答控制在 90 秒左右，讲完主动停下。

## 一句话结论

> 实习中最困难的不是修某一个 Markdown badcase，而是在输入不稳定、问题归属不清、修复又可能误伤正常内容的情况下，把一个模糊问题推进成可分类、可取舍、可验证的治理闭环。

## 推荐回答：约 90 秒

> 我在字节实习中遇到最困难的一件事，是大模型 Markdown 渲染质量治理。最初大家看到的只是标题、列表、表格等显示异常，但原因可能在模型输出、解析器或前端；parser 还常常只降级成普通文本而不报错，所以问题和责任边界都不清楚。
>
> 我没有针对单个 case 打补丁，而是主动汇总 QA 和巡检发现的 badcase，并和相关同学对齐 Success、Fail、Worse 三类口径。最大的权衡是：规则越激进，表面异常率可能越低，但越容易修坏正常内容。因此我选择用 AST 限定上下文，再做最小规则修改；低置信内容宁可保留原文，也不强行改写。
>
> 推进中，我逐轮同步修复范围、Worse 和未解决项，明确哪些由前端兜底、哪些带着样本和分类反馈上游，并把新问题沉淀进回归集，而不是转交后等待。最终异常率从 7.64% 降到 3.23%。这让我认识到，负责不只是写完代码，还要主动统一口径、暴露风险并推动各方形成可验收的结果。

## 精简回答：约 60 秒

> 实习中最困难的是大模型 Markdown 渲染治理。难点不在某个 bug，而在于异常可能来自模型输出、解析器或前端规则，同时修复过强又会误伤正常内容。
>
> 我先主动汇总并分类 badcase，和相关同学统一 Success、Fail、Worse 的验收口径；技术上没有选择简单的全局正则或激进修复，而是用 AST 限定上下文，再做最小规则修改，低置信内容保留原文。每轮我都会同步修复范围、Worse 情况和上下游边界，并把新样本沉淀进回归集。最终异常率从 7.64% 降到 3.23%。最大的收获是，面对模糊问题要主动定义标准、显性化风险，并对最终闭环负责。

## 结构拆解：主管听到的四层信息

### 1. 为什么困难

- 输入是大模型生成内容，格式不稳定且长尾很多。
- 同一现象可能来自模型、解析器或前端，责任边界起初不清楚。
- parser 失败时可能只是退化为 Text，没有明确报错可依赖。
- 修复和误伤存在直接冲突，不能只追求更高的表面命中率。

### 2. 你做了什么权衡

| 选择 | 短期收益 | 主要风险 | 最终判断 |
| --- | --- | --- | --- |
| 全量原文正则 | 实现快、覆盖看起来高 | 误伤代码块、公式、HTML 和正常符号 | 不采用 |
| 激进自动改写 | 异常率可能快速下降 | 改变原意，Worse 成本更高 | 不采用 |
| 在线 LLM Repair | 能理解复杂语义 | 延迟、成本、结果不确定、可能改写原意 | 不进主链路 |
| AST 限定上下文 + 局部最小规则 | 可解释、可回归、可灰度 | 需要维护分类和规则边界 | 采用 |

权衡总结：

> Fail 只是没有修好，Worse 是把正确内容修坏；因此我选择保守、可解释、可回滚的方案，宁可暂时少覆盖，也不以误伤换取漂亮的异常率。

### 3. 你怎么交流和推进

不要泛泛说“我积极沟通”，要说清沟通产物：

```text
QA / 巡检提供 badcase
→ 我汇总分类并补充可复现样本
→ 和相关同学统一 Success / Fail / Worse
→ 每轮同步修复范围、Worse 和未解决项
→ 前端可控问题进入规则，上游问题带样本和分类反馈
→ 新 case 沉淀进回归集，继续验证下一轮
```

适合口述的表达：

> 我不是只在群里说“这个问题不是前端的”。对于需要上游处理的内容，我会带上最小样本、异常分类、期望结果和前端继续兜底的风险；对于前端能确定处理的部分，我就直接给出规则、回归结果和回退边界。这样沟通对象只需要确认少数关键判断，项目不会因为归属不清停住。

这句话体现的是推进方法。若面试官追问具体参与方、会议或时间点，只讲自己能够确认的真实经历，不补造细节。

### 4. 你如何体现负责

- 没有把问题简化成“修几个页面 bug”，而是先建立问题地图和验收标准。
- 不只对修复数量负责，也主动对 Worse、回归和原意风险负责。
- 不因涉及上游就简单转交，而是携带证据、边界和建议继续推进。
- 结果用统一数据集验证，稳定口径是异常率 `7.64% → 3.23%`。
- 清楚说明个人边界：自己负责分类、评价口径、前端规则和推进闭环，不把模型、parser 或整个平台说成个人从零设计。

## 高频追问

### Q1：为什么这是最困难的，而不是技术上最复杂的项目？

> 因为它同时有三个不确定性：原因不确定、责任边界不确定、修复副作用也不确定。单个规则并不一定复杂，难的是在开放输入下确定什么应该修、由谁修、怎样证明没有修坏，并推动相关角色按同一标准完成闭环。

### Q2：你在这里最关键的个人贡献是什么？

> 我最关键的贡献不是某一条规则，而是把零散 badcase 变成了分类、评价口径和可回归的治理流程，并在这个基础上完成前端可控部分的 AST 上下文修复。这样后续讨论不再只凭个别截图，而是能比较 Success、Fail 和 Worse，再决定规则是否值得上线。

### Q3：你和其他人有分歧吗？

> 更准确地说，主要是目标之间的张力，而不是人与人的激烈冲突。一种直觉是尽量多修、尽快压低异常率；我的关注点是不能因此提高 Worse。为了避免停留在观点争论，我把正常样本和负向指标一起放进评测，用同一批 case 比较不同方案，让数据决定规则是否进入主链路。

不要回答“某位同事坚持错误方案，最后被我说服”，现有事实不足以支持这种说法。

### Q4：相关同学不及时反馈怎么办？

> 我会把开放式问题压缩成一两个容易确认的决策点，同时附上样本、我的判断、默认处理方式和风险。例如明确哪些 case 我会按前端最小兜底继续推进，哪些低置信内容暂时保留原文。如果关键风险仍没有结论，我会在约定时间前同步负责人，而不是无限等待或自行扩大修复范围。

### Q5：工期很紧时，你还会做这么完整的分类吗？

> 会控制范围，但不会省掉负向验证。紧急情况下先处理高频、确定性强且有明确回退路径的规则，同时保留开关；长尾和低置信 case 进入后续迭代。因为如果只追求短期覆盖而不看 Worse，回归造成的返工和用户风险可能更大。

### Q6：为什么不用更强的模型在线修复？

> 在线 LLM 对复杂语义可能更灵活，但会增加延迟、成本和不确定改写风险。这个场景处在渲染主链路，稳定性和可回归更重要，所以线上采用确定性规则；模型更适合离线辅助分析和 double-check，不能代替主链路的确定性边界。

### Q7：结果怎么证明？

> 我使用同一评价口径比较方案，不只统计修好的 Success，也单独统计未修复的 Fail 和被修坏的 Worse。稳定可讲的数据是异常率从 7.64% 降到 3.23%，绝对下降 4.41 个百分点。没有证据时不额外声称用户留存、满意度或线上收入提升。

### Q8：这件事有什么不足？

> 如果重新做，我会更早冻结一批正常内容和高风险语法作为负向回归集，并让每条规则绑定来源 case、适用边界和回退方式。这样新增规则时可以更快判断它是在解决一类问题，还是只对单个样本过拟合。

### Q9：这段经历和中兴软件开发岗位有什么关系？

> 场景不同，但工程方法是共通的：面对模糊故障先建立证据和分类；在覆盖率、稳定性和成本之间做取舍；跨角色协作时明确输入、输出和责任边界；最后用回归和指标对交付结果负责。云计算软件同样需要处理开放输入、依赖故障和改动风险。

## 如果主管要求按 STAR 明确标注

**S，背景：** 大模型 Markdown 内容持续出现渲染异常，来源跨模型输出、解析器和前端，且强修复可能误伤正常内容。

**T，任务：** 我需要把零散异常收敛成可分析、可修复、可评价的问题，并负责前端可控部分的治理闭环。

**A，行动：** 汇总分类 badcase；对齐 Success/Fail/Worse；选择 AST 上下文加最小规则；逐轮同步修复范围、误伤和上下游边界；把新样本沉淀进回归集。

**R，结果：** 异常率从 7.64% 降到 3.23%，并形成了后续新问题可复用的分类和回归方法。

## 口述时不要踩的坑

- 不要从 AST、parser 或正则细节开始，第一句话先回答“难在哪里”。
- 不要说“我协调了很多部门”却讲不出沟通产物；讲样本、口径、边界、回归和风险。
- 不要把正常方案讨论包装成人际冲突。
- 不要只讲异常率下降，必须带出 Worse 和保守修复的权衡。
- 不要连续说多个假设场景；讲完一个真实过程和结果就停。
- 少说“我理解、然后的话、这一块、比如说”，每段只保留一个中心句。
- 不要把整个模型、Markdown parser 或平台说成自己设计；个人主线是分类、评测、前端规则和推进闭环。

## 脱稿关键词

```text
难点：来源不清 + 不报错 + 修复会误伤
责任：把零散 badcase 变成治理问题
沟通：样本分类 + 三类口径 + 上下游边界
权衡：Fail < Worse，宁可少修、不强改
方案：AST 上下文 + 最小规则 + 回归
推进：逐轮同步，不转交后等待
结果：7.64% → 3.23%
收获：定义问题、显性风险、负责闭环
```

一句口诀：

> 先把问题说清楚，再把风险摆出来；用最小方案推进，用统一指标负责到底。
