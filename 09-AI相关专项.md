# AI 相关专项小抄

## 我的 AI 经历怎么定位

| 经历 | 定位 | 面试口径 |
| --- | --- | --- |
| AI Agent 兴趣教练平台 | 个人完整项目 | 可重点讲多 Agent、工具调用、流式输出、上下文与 BFF |
| AI Native Bug 复盘系统 | 离线 benchmark 和方案验证 | 不说成已接入生产、自动发布 Wiki |
| 字节 AI 自动化巡检 | 实习中参与的探索与流程建设 | 讲场景、约束和工程思路，不虚构个人实现规模 |
| Markdown Repair | 真实质量治理 | LLM 只用于离线 double-check，线上主链路是确定性规则 |

## 30 秒 AI 自我定位

> 我对 AI 的关注点不是简单接一个聊天接口，而是如何把模型的不确定能力放进可控工程链路。我做过多 Agent 和 Function Calling，也做过 Schema 约束、证据绑定、RAG、上下文压缩、流式渲染和离线评测。我的原则是：模型负责语义理解和生成，传统工程负责权限、状态、校验、重试、可观测性和最终兜底。

## 什么是 AI Native

AI Native 不是在原产品旁边增加聊天框，而是让模型参与原本由固定规则或人工完成的业务流程，例如：

- 理解非结构化输入。
- 拆解任务并选择工具。
- 根据上下文持续决策。
- 生成结构化结果并进入后续系统。

判断一个需求是否适合 AI：输入是否具有语义不确定性、规则是否难以穷举、错误成本是否可控、是否存在可验证结果和人工兜底。

## 多 Agent 为什么不用单模型

- 复杂任务同时包含规划、执行、校验和表达，一个 Prompt 容易职责冲突与长链路漂移。
- Planner 负责拆解，Critic 负责检查，Host 控制流程，Reporter 汇总结果；每个角色的输入输出更容易单独验证。
- 代价是 token、延迟和调度复杂度，因此简单问答仍用单模型。
- 多 Agent 的价值不是“角色多”，而是职责隔离、失败恢复和可观测性更强。

## Host 决策机制怎么讲闭环

> 我第一版的问题是 Host 返回了 `next_agents`，但编排器下一轮还是固定执行 Planner 和 Critic；`converge` 生成的约束也会因为立即退出而永远不执行。后来我把 Host 改成确定性状态机：模型 Agent 负责生成和评审，代码负责调度和终止。

Host 的主要输入：

- Critic 的 `feasible / realistic / complete`。
- 未解决 high risk 列表和结构覆盖率。
- 高风险数量、覆盖率在多轮中的变化，用于判断真实进展和停滞。
- 语义相似度只辅助识别首轮过快一致或重复，不直接表示质量。

五个动作：

| 动作 | 下一步 | 含义 |
| --- | --- | --- |
| `revise` | Planner | 逐项修订具体高风险或缺失项 |
| `challenge` | Critic | 首轮过快一致时做最坏情况压力测试 |
| `verify` | Critic | 验证 Planner 的修订是否真正关闭问题 |
| `finalize` | Reporter | 所有结构化质量门槛通过 |
| `terminate` | Reporter | 达到预算，保留并披露未解决风险 |

关键闭环：

- Orchestrator 下一轮严格执行 `next_agents`，不再固定跑全套。
- `must_address` 会进入下一 Agent 的 Prompt，不再只是展示。
- 轮次预算允许时，Planner 单独修订后必须再经过 Critic，不能直接收敛；最大轮次优先 `terminate`。
- Host 趋势状态写入会话检查点，断点恢复后不会从空趋势开始。

## Function Calling 是什么

模型不直接执行函数，而是根据工具描述生成工具名和结构化参数；宿主程序完成权限检查、参数校验和真实调用，再把结果返回模型继续推理。

安全边界：

- 工具白名单和最小权限。
- JSON Schema/类型校验。
- 超时、重试和幂等键。
- 高风险操作需要人工确认。
- 限制最大轮次，防止循环调用。
- 完整记录调用链和 token/耗时。

## 如何降低幻觉

1. **输入约束**：只提供相关证据，过滤噪声。
2. **输出约束**：Schema、枚举和必填字段。
3. **证据约束**：关键结论必须绑定 `source_ids`。
4. **工具约束**：参数校验、权限、规则引擎二次判断。
5. **流程约束**：最大轮次、终止条件、失败降级。
6. **结果约束**：离线评测、人工审核、高风险操作确认。

一句话：Prompt 只能降低概率，不能提供确定性保证；可靠性来自模型约束与传统工程共同兜底。

## RAG 为什么不只用向量检索

- Embedding 适合语义相似，例如“stale closure”和“状态读取旧值”。
- BM25 适合错误码、模块名、接口名和文件路径等精确 token。
- 故障知识库同时需要两类信号，因此采用 BM25 + Embedding 混合召回，再 rerank。
- rerank 可对错误码、模块和路径命中加分，对“误判、已排除”等 hard negative 降权。

口径隔离：`51% / 55% / 94%` 是 AI Native Bug 复盘材料中的合成事故样本结果，不是兴趣教练项目的实测数据。兴趣教练当前只完成了混合排序、降级和调度的单元/回归测试，尚未建立足够规模的人工标注召回集；面试时不能混用两组数据。

## 长上下文与记忆

### AI Agent 对话

- LocalStorage 只保留最近 10 轮完整消息用于秒开；MongoDB `messages` 保存完整原文，压缩任务不删除事实源。
- 历史消息异步切成 `memory_items`：默认 1200 字符一块、重叠 120 字符，并生成 Embedding。
- `memory_summaries` 保存目标、偏好、约束和自然语言摘要，每条都带 `sourceMessageIds`，可追溯、可重建。
- 长期召回同时使用 BM25/关键词和 Embedding；RRF 融合名次后，再按时间衰减与重要性重排。
- 构建上下文前先扣除输出预留、安全边界、system prompt、当前消息和工具 Schema；最近两轮原文强制保留，其余摘要/记忆按相关性与 token 成本装箱。
- Embedding、摘要不可用时自动降级到全文/关键词原文检索，派生层失败不阻断消息落库和下一次提问。
- API 返回的 `total_tokens` 是累计成本口径，不能和上下文窗口直接比较；`lastInputTokens` 看压力，`lifetimeBillableTokens` 看成本，`unsummarizedTokens` 决定何时生成新摘要。

一句话讲 RRF：两路分数的量纲不同，直接相加难调；RRF 根据“各自排第几”做融合，更容易稳定组合词法与语义召回。

默认重排口径：相关性 0.7、时间 0.1、重要性 0.2，时间半衰期 30 天，最终补充 5 条历史。它们是待离线集调优的工程默认值，不是已证明的最佳参数。

数据分层：

| 集合 | 职责 | 一致性 |
| --- | --- | --- |
| `messages` | 原始消息事实源、会话展示 | 主链路同步写 |
| `memory_items` | 切片、Embedding、检索和重排 | 后台异步派生，可重建 |
| `memory_summaries` | 摘要、目标、偏好、约束、来源消息 ID | 后台异步派生，可重建 |
| `conversation_token_states` | 上轮输入压力、累计账单、未摘要增量、压缩状态 | 每轮写 usage；原子抢占摘要任务 |
| `multi_agent_sessions` | Agent history、下一调度、Host 趋势检查点 | 短期状态，TTL 清理 |

如果问“为什么不全放 messages”：一条消息可能拆成多个检索块，Embedding 和摘要还需要模型版本、状态、来源范围和重建；拆开后升级派生记忆不污染事实源。代价是最终一致性；当前实现了运行/失败状态、延迟重试和超时重抢，生产化仍应补持久队列或 Outbox、积压监控和摘要质量评测。

### Bug 复盘

- Evidence Filter 保留 error、warning、release window、根因、影响和高风险 diff。
- Map-Reduce 对分块证据先压缩，再聚合成结构化输入。
- 评价压缩不能只看 token，还要看 Gold Evidence Recall 和根因准确率。

材料中的离线口径：Evidence Filter 平均输入 token 降低 23.3%，Map-Reduce 降低 37.3%，同时关键证据召回保持 100%。只说是小规模离线 benchmark。

## AI 巡检为什么不只用传统 E2E

- E2E 擅长确定性验证：元素是否存在、接口是否成功、流程是否可达。
- AI 更适合固定规则难覆盖的视觉错乱、语义异常和体验问题。
- AI 巡检是 E2E 的补充，不是替代。
- 典型链路：执行步骤 -> 页面截图/结构信息 -> 模型判断 -> 规则复核 -> 报告/人工确认。
- 误报和漏报都要用数据集评测；不能用一次 demo 判断有效。

## 为什么必须人工审核

- 错误知识进入团队 Wiki 会被长期复用，污染成本高。
- AI 生成结构化草稿和相似案例，人工只检查根因、影响和修复建议等高风险字段。
- 通过原始证据与 AI 结论的 Diff、citation 和置信信息降低审核成本。
- 人工审核不是方案失败，而是根据错误成本设计的人机边界。

## 如何评测 AI 系统

### 结构化抽取

- Parse Success、字段完整率、字段/根因准确率。
- Citation Support、Unsupported Claim Rate。

### RAG

- Recall@K：相关内容有没有被召回。
- MRR/NDCG：相关内容排得是否靠前。
- 加入 hard negative，防止只在简单正样本上自嗨。

### Agent

- 任务完成率、工具调用正确率、平均轮次、失败恢复率。
- P95 延迟、token 成本、超时率。
- 高风险错误单独统计，不能只看平均成功率。

## AI 场景中的 BFF

- API Key、Prompt、模型路由和工具权限不能下发浏览器。
- BFF 统一做鉴权、上下文拼接、模型切换、限流、日志和错误映射。
- 流式场景要支持取消、超时、背压和断线处理。
- 多一跳会增加运维和延迟，因此需要 trace、成本统计与降级模型。

### BFF 方案为什么不只是“保护 Key”

> Key 保护只是最低层收益。这个项目更需要 BFF 作为 AI 控制面：浏览器只表达用户意图，服务端统一决定上下文里放哪些历史、调用哪个 Agent、下一轮执行谁、何时终止，并把供应商流转换成稳定的前端事件协议。这样模型切换、Prompt、权限和失败降级不会扩散到 UI。

代价也要主动说：多一跳会增加部署、扩缩容和故障面；如果 BFF 做大量 CPU 计算还会阻塞请求。因此流式数据尽量透传，Embedding 建索引放后台，长任务再拆队列。企业内部系统还需要工具级 RBAC、审计和幂等，而当前个人项目只实现了其中一部分。

### QPS 上升 10 倍先看哪里

1. 先用 trace 拆开 Mongo 查询、Embedding RPC、Agent 模型调用和 SSE 持有时间，不直接猜缓存。
2. 消息写入与记忆建索引已经解耦；进一步把索引任务放队列，做并发、重试和积压监控。
3. 本地 fallback 最多在有限候选上计算 BM25/余弦，只适合开发；高 QPS 切 Atlas Search/Vector Search，避免应用层扫描。
4. Agent 调用成本最高：简单请求绕开多 Agent，对相同配置做可控缓存，设置最大轮次、超时和并发配额。
5. SSE 是长连接，检查实例连接数、网关超时、断开取消和背压；不能只盯单请求 QPS。

## AI 技术方案取舍矩阵

### 规则、LLM 与混合方案

| 方案 | 优势 | 局限 | 适合 |
| --- | --- | --- | --- |
| 规则/正则 | 确定、便宜、可解释 | 难覆盖开放语义，维护规则膨胀 | 格式校验、白名单、低成本过滤 |
| LLM | 理解非结构化语义，泛化强 | 幻觉、成本、延迟和不稳定 | 归纳、分类、生成候选结果 |
| 规则 + LLM | 语义能力与确定性结合 | 链路更复杂，需要定义责任边界 | 企业 AI 主流程 |

项目里的选择：规则先做 evidence filter，LLM 做结构化归纳，Schema/规则再校验，人工负责高风险最终结论。

### Prompt、RAG 还是微调

- **Prompt/Few-shot**：需求变化快、示例少时最快；知识不会自动更新，复杂约束容易漂移。
- **RAG**：答案依赖私有或频繁更新知识，需要引用来源；代价是切分、召回、排序和权限治理。
- **微调**：适合稳定风格、分类或行为模式；不适合把频繁变化的事实“写进参数”，训练和评测成本高。
- 常见组合是 Prompt 定义任务、RAG 提供事实、微调改善稳定行为，而不是三选一。

### Free-form、普通 JSON 与 Schema

| 输出方式 | 优势 | 风险 |
| --- | --- | --- |
| Free-form | 表达自然、最快做 Demo | 格式漂移，难进入下游系统 |
| JSON Prompt | 可解析性提高 | 字段类型、缺失和 citation 仍不稳定 |
| Schema + Few-shot | 字段、类型和证据位置更稳定 | Schema 设计、版本兼容和重试成本 |

进入数据库、工具调用或自动化流程时优先 Schema；给用户写开放文案时可以保留自然语言，但关键事实仍应可追溯。

### 单模型、Workflow、Agent 与多 Agent

- 单模型：一步完成，最低延迟成本。
- Workflow：步骤固定，由代码控制，最容易测试和回滚。
- Agent：工具和步骤需动态选择，适合开放任务。
- 多 Agent：职责隔离或需要交叉评审，复杂度最高。

选择顺序应从确定性最高的方案开始。能写成 Workflow 就不要为了“智能”改成自由 Agent；多 Agent 要证明单 Agent 的失败模式确实来自职责冲突。

### 全量上下文、过滤、摘要与 RAG

| 方案 | 优点 | 风险 |
| --- | --- | --- |
| 全量输入 | 简单，显式信息不易漏 | token/延迟高，噪声影响注意力 |
| Evidence Filter | 便宜、确定，保留高信号行 | 规则可能漏掉隐式证据 |
| Map-Reduce 摘要 | 可处理长材料 | 摘要误差会累积，需评估证据召回 |
| RAG | 按问题取相关片段 | 召回失败时模型根本看不到答案 |

正确做法不是只比较 token，而是同时测 Gold Evidence Recall、根因准确率、延迟和成本。

### Embedding、BM25 与 Hybrid

- Embedding 对语义改写强，但可能忽略错误码、路径和版本号。
- BM25 对精确 token 强，但不理解表达不同的同义问题。
- Hybrid 分路召回，先用 RRF 融合排名，再叠加时间和重要性；代价是索引、权重和评测复杂度。
- 故障复盘含大量精确信号，因此选择 Hybrid，而普通自然语言 FAQ 可能向语义召回倾斜。

### 自动执行还是 Human-in-the-loop

- 错误成本低、结果可自动验证：可以更自动化，例如分类、候选标签。
- 可逆但有业务影响：模型建议 + 规则验证 + 审计日志。
- 不可逆或高风险：必须人工确认，例如发布、删除、权限和知识库最终根因。
- 人工审核的位置应由错误成本决定，不是固定放在所有步骤末尾。

### 单一大模型还是多模型路由

- 全部用大模型实现简单、质量上限高，但成本和延迟高。
- 小模型适合分类、过滤、摘要等稳定任务；大模型处理复杂根因和最终生成。
- 路由会增加评测矩阵、回退逻辑和版本管理，流量不大时未必值得。
- 先按任务离线评测质量/成本曲线，再决定路由，不只按模型单价。

## 方案取舍的统一收尾

> AI 系统里最重要的不是选最强模型，而是把不确定性放在错误成本可控的位置。确定性步骤尽量交给代码和规则，模型处理开放语义；输出进入下游前做 Schema、证据和权限校验。只有当动态决策的收益超过额外 token、延迟和调试成本时，才从 Workflow 升级为 Agent 或多 Agent。

## 高频问题一句话回答

**为什么 Schema + Few-shot？**

JSON Prompt 只要求“像 JSON”，Schema + 示例才能稳定字段结构、类型和 citation 位置。

**如何控制 token 成本？**

证据过滤、短期上下文 + 历史检索、分层摘要、缓存、多模型路由，并用质量指标确保没有用降质换成本。

**为什么保留规则？**

规则适合低成本过滤和确定性校验，LLM 适合理解与归纳，两者职责不同。

**模型超时怎么办？**

设置 deadline、取消旧任务、有限重试、降级到小模型/模板结果，并保证主业务链路可继续。

**AI 项目最大风险？**

不是某次回答不好，而是错误结果被工具调用或知识库放大，所以必须限制权限、绑定证据、记录链路并保留人工边界。

## 面试真实性边界

- 不说 AI Native 复盘系统已经线上运行；它是离线方案验证。
- 不把 20 条真实模型评测或 80 条合成样本说成大规模结论。
- 不把 Bug 复盘材料的 `51% / 55% / 94%` 召回数据说成兴趣教练项目结果。
- 兴趣教练可以说专项 3 个测试套件、13 项测试通过；不能把单元测试说成线上质量提升。
- 建 Atlas 索引和历史回填已有脚本，但尚未在生产数据执行。
- 当前有短期 `multi_agent_sessions` 检查点，不说已经有完整 run 表、消息队列和工具审计平台。
- 不说 AI 巡检替代自动化测试。
- 不说 Schema 能彻底消除幻觉。
- 不主动堆 Multi-Agent、RAG 等名词；先说明业务问题，再说明为什么需要这些技术。
