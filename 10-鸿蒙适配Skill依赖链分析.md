# 鸿蒙适配 Skill：依赖链与有限穷举

## 最终方案一句话

> 先用 Babel AST 从卡片入口展开真实依赖链，覆盖跨目录、跨组件的传递依赖；再只在当前卡片目录内，对细碎且适合确定性匹配的鸿蒙属性用法做有限穷举。两路结果合并去重后执行风险规则，只把命中代码和依赖证据交给后续 Skill 或模型。

核心思想：

> 不是在全仓库无边界地搜索，也不是强迫所有问题都用 AST 解决，而是先收缩问题空间，再在小范围内穷举。

## 两分钟讲述故事

> 这个工具是为了自动排查卡片在鸿蒙平台上的适配风险。最初的方案偏向“先尽可能展开和扫描，再通过大量条件一点点排除无关文件与误报”。这版实现代码大约有 2～3K 行，单张卡片平均会扫描 50 多个文件。虽然召回范围大，但分支和排除规则越来越多，执行成本与维护成本都比较高。
>
> 我复盘后发现，风险其实分成两类。
>
> 第一类是跨文件的传递依赖问题。例如卡片 B 引用了组件 A，真正不兼容的代码在 A 中。这类问题必须知道真实引用关系，所以适合从卡片入口出发，用 Babel Parser 把源码解析成 AST，再通过 Traverse 提取 import、export 和可以静态确定的动态 import，结合 `index.json` 业务映射解析真实路径，递归生成当前卡片的依赖闭包。
>
> 第二类是比较细碎的属性用法问题。它们通常只需要检查当前卡片目录中是否存在某种确定的属性组合或调用方式。如果为了这些规则也不断扩展通用 AST 分析和全仓依赖逻辑，代码会迅速膨胀。所以最终版对这类规则直接限制在卡片目录内做有限穷举：只扫描指定源码后缀，排除测试、产物和第三方目录，通过精确规则找出候选用法。
>
> 最终两路结果取并集并按真实路径去重：依赖链负责找到当前卡片真正可达的跨目录组件，有限穷举负责补齐卡片目录内零散、适合直接检查的属性用法。之后再执行本地风险规则，不会把“出现某个属性名”直接等同于风险，而是保留命中位置、使用方式和引用链作为证据。
>
> 这样改完后，实现代码从约 2～3K 行下降到约 500 行；单张卡片平均扫描文件数从 50 多个下降到约 15 个。它不是简单减少扫描范围，而是把不同类型的问题交给最合适的分析方式：结构关系用 AST，边界明确的局部规则用有限穷举，在准确性、性能和维护性之间取得平衡。

## 30 秒版本

> 最终方案是两条链路。主链路从卡片入口出发，用 Babel AST 提取依赖，通过自定义 Resolver 处理 `index.json` 组件映射，再递归得到真实可达文件；补充链路只在当前卡片目录中，对细碎的鸿蒙属性用法做有限穷举。两路合并去重后再跑本地规则并生成证据链。相比之前先大范围展开再逐层排除的方案，代码从约 2～3K 行降到约 500 行，平均扫描文件从 50 多个降到约 15 个。

## 演进过程与技术取舍

| 版本 | 做法 | 优点 | 问题 |
| --- | --- | --- | --- |
| 初版 | 尽可能展开和扫描，再逐层排除 | 召回范围大、实现直观 | 2～3K 行代码，分支多，平均扫描 50+ 文件 |
| 纯依赖链思路 | 所有问题都尝试通过 AST 与依赖图解决 | 可达关系准确、证据链清晰 | 细碎属性规则也被迫进入复杂分析框架 |
| 最终混合方案 | Babel 依赖链 + 卡片目录有限穷举 | 结构问题和局部规则各走合适链路 | 需要明确两类规则边界并合并结果 |

最终取舍：

- 跨文件、跨目录、需要回答“为什么这个文件与卡片有关”的问题，走依赖链。
- 属性名稳定、规则简单、检查范围天然局限在当前卡片的问题，走有限穷举。
- 动态路径、生成代码等静态分析无法确定的情况，记录为低置信度并进入兜底。

## 最终架构

```text
                         卡片入口
                            │
              ┌─────────────┴─────────────┐
              │                           │
       依赖链分析主路                 有限穷举补充路
              │                           │
        Babel Parser                 确定卡片根目录
              │                           │
        Traverse 提取依赖            过滤后缀/测试/产物
              │                           │
    index.json + 业务 Resolver        扫描细碎属性用法
              │                           │
        递归展开依赖闭包               生成命中候选
              └─────────────┬─────────────┘
                            │
                   真实路径归一化、去重
                            │
                      本地风险规则
                            │
             代码片段 + 位置 + 完整依赖证据
                            │
                     Skill / 模型判断
```

候选集合可以抽象成：

```text
最终候选文件
= 从入口可达的依赖文件
∪ 当前卡片目录中命中属性规则的文件
```

## 第一条链路：Babel AST 依赖分析

### 为什么使用 AST

正则只能匹配文本，无法稳定区分：

```ts
import A from './A';                    // 真实依赖
const example = "import A from './A'";  // 普通字符串
// import B from './B';                 // 注释
const module = await import('./module');// 动态依赖
export { C } from './C';                // 重导出
```

Babel Parser 会把源码转换成有类型的 AST 节点：

```text
ImportDeclaration
├─ specifiers
└─ source: "./A"
```

字符串中的 `import` 仍然只是字符串节点，不会被当成 `ImportDeclaration`。

### Parser 与 Traverse 分工

> Parser 负责把源码变成 AST；Traverse 负责访问 AST 中关心的节点。

概念代码：

```ts
const ast = parser.parse(code, {
  sourceType: 'unambiguous',
  plugins: ['typescript', 'jsx']
});

traverse(ast, {
  ImportDeclaration(path) {
    dependencies.push(path.node.source.value);
  },
  ExportNamedDeclaration(path) {
    if (path.node.source) {
      dependencies.push(path.node.source.value);
    }
  },
  ExportAllDeclaration(path) {
    dependencies.push(path.node.source.value);
  }
});
```

实际实现还要结合项目语法配置，处理可静态确定的动态 import、CommonJS 或项目特有写法。

### 业务 Resolver

AST 提取出的只是模块标识符：

```text
./A
@components/Button
@a组件
```

Resolver 才负责把它转换成真实文件：

```text
src/components/A.tsx
```

Resolver 需要处理：

- 相对路径。
- `.ts`、`.tsx`、`.js`、`.jsx` 等扩展名。
- 目录下的 `index` 文件。
- 项目路径别名。
- `index.json` 中的业务组件映射。

概念流程：

```ts
function resolveImport(specifier, importer, cardRoot) {
  const componentMap = readComponentMap(cardRoot);

  if (componentMap[specifier]) {
    return resolveMappedPath(componentMap[specifier], cardRoot);
  }

  return resolveStandardPath(specifier, importer);
}
```

> Babel 解决“这是不是依赖声明”，业务 Resolver 解决“它到底指向哪个文件”，两层职责不能混在一起。

### 递归展开与循环依赖

```ts
function visit(file) {
  const realFile = normalizeRealPath(file);

  if (visited.has(realFile)) return;
  visited.add(realFile);

  const dependencies = extractDependencies(realFile);

  for (const dependency of dependencies) {
    const resolved = resolveImport(dependency, realFile, cardRoot);
    addEdge(realFile, resolved);
    visit(resolved);
  }
}
```

`visited` 防止同一文件重复解析，也避免 `A → B → C → A` 导致无限递归；依赖边仍然保留，因此可以继续还原引用链。

## 第二条链路：卡片目录有限穷举

### 为什么还需要直接搜索

纯依赖链擅长回答：

> 当前卡片实际依赖了哪些文件？

但一些鸿蒙风险是细碎且字面稳定的属性组合、样式属性或调用用法。为每条小规则都维护完整的通用 AST 分析，会产生大量节点判断和例外分支，维护收益不高。

因此对这类规则：

1. 先把搜索根目录限制在当前卡片目录。
2. 只扫描允许的源码类型。
3. 排除测试、构建产物、缓存和第三方目录。
4. 对已知属性或调用模式做确定性匹配。
5. 保留文件、行列位置和代码片段。

关键区别：

> 有限穷举负责提高候选召回，不代表属性名一出现就直接判错；最终仍由对应风险规则检查它的使用方式。

### 为什么不是全仓穷举

全仓搜索的主要问题不是搜索算法本身，而是范围无边界：

- 会命中其他卡片、废弃文件和不可达组件。
- 文件数量、耗时和上下文随仓库增长。
- 无关候选会增加误报分析成本。

卡片目录是一个天然业务边界。在这个小范围内穷举，复杂度可控，也比为所有零散规则构造通用 AST 框架更容易维护。

### 为什么不是只扫描卡片目录

因为卡片可能引用目录外的公共组件：

```text
CardB
  └─ import SharedA
               └─ 鸿蒙风险
```

只扫描卡片目录会漏掉 `SharedA`。依赖链负责向外找到真实可达的公共组件，卡片目录穷举只负责补齐局部属性规则，两者不能互相替代。

## 两路结果如何合并

```ts
const candidates = new Map();

for (const file of dependencyFiles) {
  candidates.set(normalizeRealPath(file), {
    file,
    sources: ['dependency']
  });
}

for (const hit of propertySearchHits) {
  const key = normalizeRealPath(hit.file);
  const previous = candidates.get(key);

  candidates.set(key, {
    file: hit.file,
    sources: [...(previous?.sources ?? []), 'property-search'],
    propertyHits: [...(previous?.propertyHits ?? []), hit]
  });
}
```

需要注意：

- 使用规范化后的真实路径去重，避免相对路径、alias 和大小写差异。
- 同一文件可能同时来自依赖链和属性扫描，来源信息都要保留。
- 依赖链负责说明文件为什么与入口有关。
- 属性扫描负责说明文件中哪个位置命中了哪条规则。

## 如何生成证据链

遍历依赖图时维护父节点，或者在生成图后执行路径搜索：

```text
CardEntry.tsx
  → Container.tsx
  → SharedComponent.tsx
  → 命中鸿蒙风险属性
```

最终输出不只包含“哪个文件有问题”，还应该包含：

```json
{
  "riskFile": "SharedComponent.tsx",
  "rule": "harmony-property-usage",
  "location": {
    "line": 42,
    "column": 8
  },
  "dependencyChain": [
    "CardEntry.tsx",
    "Container.tsx",
    "SharedComponent.tsx"
  ],
  "snippet": "..."
}
```

这让模型和人工审核都能回答：

- 为什么扫描这个文件？
- 哪条规则命中了？
- 风险代码在哪里？
- 当前卡片通过什么路径引用它？

## 最终版的可量化结果

| 指标 | 旧版 | 最终版 | 变化 |
| --- | ---: | ---: | ---: |
| 实现代码量 | 约 2～3K 行 | 约 500 行 | 下降约 75%～83% |
| 单卡平均扫描文件 | 50+ | 约 15 | 至少下降约 70% |

正确表述：

> 代码量下降不是单独的成功指标，它代表我删除了大量通用排除分支；再结合平均扫描文件从 50 多个降到约 15 个，说明分析边界确实得到了收缩。最终还需要用同一套回归集重新验证召回率和误报率，不能直接沿用旧版本的准确率数据。

面试前最好继续补齐：

- 单卡扫描耗时的 P50/P95。
- 属性扫描与 AST 依赖分析各自耗时。
- 平均依赖节点数和依赖边数。
- 未解析依赖数量。
- 风险规则的精确率、召回率。
- 发送给模型的 token 数变化。

## 为什么最终没有直接依赖 dependency-cruiser

> 我调研过 dependency-cruiser，它的 Parser、Resolver、依赖图、循环检测和规则输出比较完整，适合做通用仓库依赖治理。但我们的核心问题一部分是 `index.json` 的业务路径映射，另一部分是卡片目录内的细碎鸿蒙属性规则。如果直接引入完整依赖分析框架，仍然需要写业务 Resolver 和属性扫描，而且能力比当前场景需要的更重。最终我保留了它“解析、解析路径、构图分层”的设计思路，但落地使用 Babel 加一层轻量 Resolver，再配合有限目录搜索，约 500 行即可覆盖核心需求，维护边界更清楚。

如果被问“是不是重复造轮子”：

> 我没有自己实现 JavaScript Parser，而是复用 Babel；自研部分只包括项目特有的 `index.json` Resolver、遍历控制和鸿蒙规则。dependency-cruiser 解决的是更通用的仓库依赖治理问题，我们最终需要的是一个嵌入 Skill 的轻量单卡分析工具，所以选择了更小的组合。

## 高频追问

### 1. 为什么从纯依赖链改成混合方案？

> 因为两类问题的结构不同。跨文件传递依赖必须通过 AST 和依赖图处理；细碎、字面稳定、范围天然局限在卡片目录的属性规则，有限搜索更简单。强行统一成一种分析方式会增加代码和维护成本。

### 2. 为什么不继续用“先全部展开，再逐步排除”？

> 这种方式会让每新增一种例外都增加一层过滤分支，扫描范围和代码复杂度持续增长。最终版先按问题性质划分边界，从源头决定哪些文件需要进入哪条分析链路。

### 3. 直接搜索会不会退化回最初的全仓搜索？

> 不会。最初搜索范围是全仓，最终版的搜索范围被限制在当前卡片目录，而且只用于少量适合确定性匹配的属性规则。跨目录部分仍然由入口依赖链控制。

### 4. 卡片目录中有未使用文件，会不会误报？

> 存在这种可能，所以有限穷举主要负责召回候选，不能把属性名出现直接等同于错误。结果会记录来源，如果文件不在依赖闭包内，可以降低置信度、检查配置引用，或者要求规则提供更明确的使用证据。

### 5. 为什么不用 AST 分析所有属性？

> AST 可以做，但不是每条规则都值得为它维护复杂节点逻辑。属性名称和组合稳定、检查范围很小时，直接规则更短、更清晰；需要理解嵌套结构、调用参数或作用域的规则仍然应该走 AST。

### 6. 如何判断一条规则走 AST 还是搜索？

| 特征 | 推荐方式 |
| --- | --- |
| 需要理解 import、作用域、调用参数、嵌套结构 | AST |
| 需要跨目录还原组件引用关系 | AST + Resolver |
| 属性字面稳定、范围小、上下文要求低 | 有限搜索 |
| 动态生成、运行时注册 | 静态分析标记 + 兜底 |

### 7. Babel Parser 和 Traverse 分别是什么？

> Parser 把源码转成 AST；Traverse 按节点类型遍历 AST。Parser 解决语法识别，Traverse 负责提取我关心的 import、export 等节点。

### 8. Babel 能直接找到真实文件吗？

> 不能。Babel 只能得到 `./A` 或 `@a组件` 这样的模块标识符。真实路径需要业务 Resolver 根据引用文件、扩展名、目录入口和 `index.json` 映射解析。

### 9. `index.json` 映射为什么不能写成普通 alias？

> 如果不同卡片下同一个组件名可能映射到不同目录，它就是与卡片上下文相关的映射，而不是一份全局固定 alias。Resolver 必须知道当前入口或当前卡片根目录。

### 10. 动态 import 怎么处理？

```ts
import('./module');      // 静态字符串，可分析
import(`./module`);      // 无变量，可分析
import(`./${name}`);     // 运行时决定，无法完全静态分析
```

> 能静态确定的正常加入依赖图；不能确定的记录为 unresolved dynamic dependency，触发有限范围扩大或人工兜底，不随意猜路径。

### 11. 怎么防止循环依赖导致死循环？

> 用规范化真实路径作为 `visited` 的 key。同一文件只解析一次，但依赖边继续保留，因此既不会死循环，也能还原循环和引用关系。

### 12. 平均扫描文件为什么会从 50+ 降到约 15？

> 旧版先扩大候选再排除，很多文件进入分析后才被过滤；最终版从入口依赖和卡片目录两条明确边界产生候选，排除逻辑前移，因此进入深度检查的文件更少。

### 13. 500 行一定比 2～3K 行好吗？

> 不一定，所以不能只讲代码行数。我同时关注扫描文件数、耗时、召回和误报。代码量下降的价值是减少了重复分支与例外规则，只有在回归效果不下降的前提下才说明维护性真正改善。

### 14. 两条链路会不会重复扫描？

> 会有交集，所以使用真实路径归一化后的 Map/Set 去重。同一文件只做一次深度规则分析，但保留它来自依赖链、属性搜索或两者同时命中的来源。

### 15. 如何定义卡片目录，防止扫描越界？

> 由卡片入口或 `index.json` 明确确定根目录，路径先规范化并校验仍位于根目录内；只允许白名单源码扩展名，并排除测试、产物、缓存和第三方目录。

### 16. 如果公共组件在卡片目录外怎么办？

> 依赖链会从入口沿真实 import 找到目录外的公共组件，这正是保留 AST 依赖分析的原因。有限穷举只补充卡片目录内规则，不承担跨目录依赖发现。

### 17. 为什么不把两路结果全部交给模型？

> 模块解析和确定性属性规则适合本地代码完成。模型只接收命中的最小代码片段、规则和依赖链，可以降低 token、延迟以及无关上下文对判断的影响。

### 18. 如何验证新旧方案准确性？

> 固定同一批人工标注卡片，分别统计卡片级目标命中率、规则精确率、召回率和未解析依赖；同时记录扫描文件数、耗时和 token。旧数据可以作为历史参考，但最终版修改了候选生成逻辑，必须重新跑回归，不能直接复用旧准确率。

### 19. 如果让你继续优化，会做什么？

- 基于文件内容哈希缓存 AST 与解析结果。
- Git diff 驱动增量扫描，只重新分析变更节点和受影响上游。
- 为动态依赖和非依赖链候选增加置信度。
- 将属性规则做成声明式配置，统一记录命中数、误报和版本。
- 分阶段记录解析、Resolver、属性扫描、规则和模型耗时。

### 20. 这个方案最大的边界是什么？

> 静态分析无法完整覆盖运行时拼接路径、动态注册、接口下发组件和构建生成代码；有限目录扫描也可能看到未实际使用的文件。因此需要 unresolved 记录、置信度和逐级扩大范围的兜底，而不是宣称完全覆盖。

## 追问树

```text
为什么做最终重构？
├─ 旧版 2～3K 行、平均扫描 50+ 文件
├─ 先扩张再排除，分支持续膨胀
└─ 风险其实可以拆成两类
   ├─ 跨文件关系 → Babel AST + Resolver + 依赖链
   └─ 细碎属性规则 → 卡片目录有限穷举

为什么这样更准、更好维护？
├─ 依赖链覆盖目录外的真实可达组件
├─ 局部穷举补齐属性用法
├─ 两路真实路径去重
└─ 约 500 行、平均约 15 个文件

继续深挖
├─ Parser 和 Traverse 区别
├─ index.json 如何解析
├─ 动态 import 怎么办
├─ 为什么不用 dependency-cruiser
├─ 如何防循环和重复
├─ 卡片目录未使用文件会不会误报
└─ 如何重新评测召回、误报、耗时和 token
```

## 面试真实性边界

- 不再把 dependency-cruiser 说成最终生产实现；它是调研和设计参考。
- 最终实现明确使用 Babel Parser/Traverse 和自定义业务 Resolver。
- 不把有限搜索说成全仓扫描，它有明确的卡片目录边界。
- 不把搜索命中直接等同于风险，区分候选召回和规则判断。
- 修改候选生成方案后，需要重新跑准确性回归；没有重跑前不沿用旧版 80% 或 20/20 数据。
- 代码量和扫描文件数可以按真实测量说明，耗时、token 和准确率没有数据就不要编。

## 最后收尾

> 这个重构的关键不是从一种技术换成另一种技术，而是重新划分问题边界：依赖关系属于结构问题，用 Babel AST 和 Resolver；细碎属性属于小范围确定性问题，用有限穷举。先通过业务边界把空间收缩，再在局部换取召回，最终将实现从 2～3K 行降到约 500 行，平均扫描文件从 50 多个降到约 15 个，同时让每条规则的责任更加清晰。
