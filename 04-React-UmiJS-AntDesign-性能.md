# React、UmiJS、Ant Design 与性能

## 本次 AI 模拟面试：详细示例回答（原文）

### 第 8 题：面向业务系统的前后端协作设计

我会把这个状态流转设计成“前端发起动作，但状态最终以后端返回为准”的链路，不会在前端自己把待审核改成已发货。

具体到这次点击，列表接口里我希望后端除了返回生产单的基础字段，比如 orderId、status、statusText、updatedAt，还返回 availableActions，比如当前用户对这条单据是否能执行 ship、approve、reject 这些动作。前端只根据 availableActions 决定按钮是否展示或置灰，但这只是体验层面的控制。

用户点击“发货”时，我会先弹一个确认框，如果有业务需要就让用户填写发货备注或物流信息。确认后前端把按钮置为 loading，避免重复点击。接口上我倾向于设计成一个明确的状态流转接口，比如：POST /production-orders/{orderId}/actions/ship，或者更通用一点 POST /production-orders/{orderId}/transitions。请求体里会带 actionCode，比如 ship，fromStatus，比如 pending_review，targetStatus，比如 shipped，以及 version 或 updatedAt，再带 reason、物流信息这类业务参数。用户身份和权限不从前端传，而是后端从 token 或 session 里取，避免前端伪造。

后端这边我会和同学约定两个关键点：第一，后端必须按状态机校验这次流转是否合法，比如 pending_review 是否真的允许直接到 shipped，如果业务上必须先审核，那应该返回一个业务错误，而不是前端自己判断；第二，后端要用 version 做乐观锁或者类似 CAS 的更新，保证这条单据在我点击的时候如果已经被别人改过，不会被我的请求覆盖。

成功以后，我希望接口直接返回这条单据的最新快照，包括 orderId、status、statusText、version、updatedAt、availableActions，以及 auditId 或 traceId。前端收到后只用这个返回值更新当前行，或者失效当前 query 后重新拉取列表。对于详情页也是一样，用返回的最新状态刷新顶部状态、按钮区和操作记录。这样前端页面和后端状态能保持一致。

异常情况我会分开处理。如果是 403，就说明当前用户没有权限，我会提示无权限并刷新按钮状态；如果是 409，说明状态或版本冲突，比如别人已经审核或发货了，我不会继续把页面改成已发货，而是提示“单据状态已变化，已为你刷新”，然后重新拉取当前行；如果是 400 的业务错误，比如状态流转不合法，就展示后端返回的可读 message。

测试上我会重点测三类：一类是不同 status 和 availableActions 下按钮是否正确展示；一类是点击后 loading、防重复提交、成功刷新和失败回滚；还有一类是 403、409 这种前后端状态不一致的场景。联调时我也会让后端提供几组固定 mock 数据，比如待审核、有权限、无权限、已被别人更新过，这样能把状态流转链路提前测完整。


## React 核心

### 什么时候重新渲染

- 自身 state 更新。
- 父组件重新渲染，子组件默认也会执行；是否提交 DOM 取决于差异。
- 订阅的 Context 值变化。
- render 阶段应保持纯：相同 props/state/context 得到相同 JSX。

### 两次加一

```js
setCount(c => c + 1);
setCount(c => c + 1);
```

`count` 是某次渲染的快照，两次 `setCount(count + 1)` 都基于旧值。函数式 updater 会进入队列，第二次使用第一次的结果。

### Effect 与副作用

- Effect 用于与 React 外部系统同步：网络、订阅、定时器、DOM/三方库。
- cleanup 应对称地取消订阅、清除定时器或中止请求。
- StrictMode 在开发环境会额外执行一次 setup + cleanup，用来暴露缺失清理；不要依赖执行次数。
- 没有外部系统时，优先考虑事件处理、派生值或 reducer，而不是 Effect。

### 三个记忆化工具

| 工具 | 作用 | 注意 |
| --- | --- | --- |
| `React.memo` | props 未变时跳过组件重渲染 | Context/state 变化仍会渲染 |
| `useMemo` | 缓存计算结果 | 只用于可测得的昂贵计算或稳定引用 |
| `useCallback` | 缓存函数引用 | 常与 memo 子组件或依赖数组配合 |

不要“全项目 memo”；比较、缓存和依赖维护也有成本。

## UmiJS 最小知识框架

- Umi 是插件化的企业级 React 框架，内置路由、构建、部署、测试和 Lint 等能力。
- 支持约定式路由和配置式路由：简单页面用约定降低配置；复杂权限、嵌套布局和重定向用显式配置更清楚。
- Umi Max/最佳实践通常把请求、权限、初始状态、数据流、国际化、Ant Design 等企业应用能力整合起来。
- 面试重点不是背 API，而是说明它解决了团队级约束、工程一致性和插件扩展问题。

### 生产/销售系统的路由与权限

> 登录后获取用户和权限 -> 生成可访问菜单/路由 -> 页面入口做前端体验层拦截 -> 后端接口仍做最终鉴权。按钮级权限不能只靠隐藏按钮，服务端必须校验操作权限。

## Ant Design 场景题

### 复杂 Form

- 明确受控字段、默认值和服务端数据回填时机。
- 大表单按业务区域拆组件，但数据域保持清晰；动态列表要用稳定 key。
- 异步校验处理竞态，可用 AbortController 或请求版本号丢弃旧结果。
- Modal 关闭后是否保留表单状态要按业务决定，避免旧数据污染下一次打开。

### 大 Table

- 先区分数据量问题与组件重渲染问题。
- 服务端分页/筛选适合可分段业务；连续浏览或固定视口的大量行可考虑虚拟列表。
- `columns`、`rowKey`、回调和展开状态保持稳定；避免每个单元格重复做昂贵计算。
- 固定列、动态行高、展开行和虚拟滚动组合时要重点测边界。

## React 与企业后台方案取舍

### 状态放在哪里

| 方案 | 适合 | 不适合/代价 |
| --- | --- | --- |
| 组件 local state | 只有当前组件树局部消费 | 跨远距离共享会层层传递 |
| 状态提升 + props | 少量父子组件共享，数据流需清晰 | 层级深时 props drilling |
| Context | 主题、用户、权限等低频全局值 | 高频变化会扩大渲染影响面 |
| 外部 store | 跨页面复杂状态、需要调试与订阅切片 | 引入额外概念和同步成本 |
| URL/query | 筛选、分页、可分享/可回退状态 | 不适合敏感或高频临时状态 |
| 服务端缓存层 | 请求数据、缓存、失效和重试 | 不应拿来保存纯 UI 开关 |

判断顺序：谁拥有数据、谁消费、是否需要跨页面、是否需持久化、刷新/回退后是否应保留。

### 派生值、state、ref 怎么选

- 能由 props/state 同步计算出的值直接派生，避免重复 state。
- 会影响 UI 且随时间变化的值放 state。
- 不参与渲染、只需跨渲染保存的实例值放 ref，例如计时器和最新请求标识。
- 不要用 ref 绕开本应触发 UI 更新的 state，也不要用 Effect 同步两份可派生状态。

面试中若结合 ReactLynx，必须再补“首屏可获得性”：

- 页面级、多个组件共享、决定首屏结构的数据：父容器整理稳定默认值后显式 props 传入。
- 用户交互后变化并驱动局部 UI 的数据：组件 state。
- 可从输入同步得到的样式或展示值：render 内局部派生。
- runtime/global/cache 不是一种 React 状态归属；共享组件在 render 内偷偷读取会让依赖和首屏时序不可见。

### Effect、事件处理与数据层

- 用户点击直接触发的动作放事件处理函数，不必绕进 Effect。
- Effect 只同步外部系统，并处理 cleanup、竞态和取消。
- 页面数据请求优先交给框架/请求缓存层管理 loading、错误、缓存和重试；手写 Effect 请求适合简单场景。
- 若使用 Effect 请求，至少用 AbortController 或版本号防止旧响应覆盖新响应。

### `memo` 还是拆状态边界

- `memo/useMemo/useCallback` 适合 Profiler 已证明的重复昂贵渲染。
- 如果一个大 Context 或顶层 state 让整页更新，更应先拆订阅和状态归属。
- 记忆化减少计算，但增加比较、缓存和依赖维护；不要把它当默认编码风格。

### Umi 约定式路由还是配置式路由

- 约定式：页面结构简单、团队遵循统一目录时，配置少、上手快。
- 配置式：复杂布局、权限、重定向、菜单元数据和特殊加载策略更清晰。
- 企业后台通常以配置式表达核心路由规则，再配合框架插件和约定降低重复代码。

### Ant Design 还是自研组件

- Ant Design 适合标准表单、表格、弹窗和企业后台交互，可降低交付与无障碍成本。
- 自研适合品牌强定制、组件行为特殊或包体/性能有明确瓶颈的场景。
- 正确路线通常是基于 Design Token 和二次封装建立业务组件层，而不是直接魔改源码。

### 分页、虚拟列表还是无限滚动

| 方案 | 产品特点 | 工程关注点 |
| --- | --- | --- |
| 服务端分页 | 数据可分段，需要定位页码和筛选 | 请求缓存、页码同步、总数变化 |
| 虚拟列表 | 连续浏览，DOM 数量大 | 动态行高、滚动锚点、overscan |
| 无限滚动 | 内容流消费，不强调页码 | 去重、断点恢复、滚动位置 |

制造/销售后台通常优先服务端分页；只有连续查看或前端已持有大量数据时再引入虚拟列表。

## 性能优化路径

1. 定义指标：LCP、CLS、INP、TTFB、长任务、交互耗时。
2. 用 Performance/React Profiler 定位，而不是先加 memo。
3. 网络：缓存、压缩、按需加载、资源优先级。
4. JS：代码分割、减少包体、Worker/分片处理重计算。
5. React：状态下沉、拆分订阅边界、稳定 key、减少无效渲染。
6. DOM：虚拟列表、避免布局抖动、按帧合并高频更新。
7. 复测并防回归，给关键指标设基线或监控。

## 企业业务系统：用一次状态流转回答

以“订单从待审核流转到已发货”为例：

```text
GET list 返回 status + availableActions + version
-> 前端据 availableActions 展示按钮
-> 点击确认，按钮 loading/置灰防重复提交
-> POST transition(orderId, fromStatus, targetStatus, version)
-> 后端做权限、当前状态和 version 的原子校验
-> 成功返回最新 order/status/availableActions
-> 前端更新当前行或失效列表缓存
-> 403/409 时提示并重新拉取，避免前后端状态分叉
```

接口契约要提前约定：分页与排序字段、状态枚举、可执行动作、权限码、版本号或 `updateTime`、错误码，以及状态流转成功后是否返回最新实体。

测试至少覆盖：

- 不同 `status/availableActions` 下按钮展示。
- loading、防重复提交、成功刷新和失败回滚。
- 403 无权限、409 被他人更新、旧请求覆盖新请求。
- 后端始终做最终鉴权；前端隐藏按钮只是体验层。

面试时先跑完这条点击链，再补组件拆分、TypeScript 类型、列配置、hooks 和权限组件；不要只讲“模块化、前端展示、后端兜底”。

## ECharts/D3 加分项的诚实答法

> 我没有把数据可视化写成核心生产经验。我的理解是，ECharts 更适合快速完成标准业务图表和交互，D3 更底层，适合高度定制的可视化。数据量大时要关注采样、增量更新、Canvas/SVG 选择、tooltip 和 resize 频率。我可以基于已有 React 性能与数据处理经验快速上手。

## 官方复习入口

- [UmiJS](https://umijs.org/)
- [React useEffect](https://react.dev/reference/react/useEffect)
- [Ant Design Form](https://ant.design/components/form/)
- [Ant Design Table](https://ant.design/components/table/)
