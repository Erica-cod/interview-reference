# 长鑫-02：半导体 AI Case 与技术问答

## 一、半导体 AI 产品 Case

### 1.1 先给场景筛选框架

如果面试官问“你会优先做什么场景”，不要一上来宣布答案。先说：

> 我会按业务价值、发生频次、数据成熟度、结果可验证性，再结合错误成本和集成成本筛选。第一阶段不会直接让模型调整设备或工艺参数，而会优先选择高频、资料密集、有人审核、能够留痕的 Copilot 场景。

推荐采用“一个快速见效场景 + 一个战略深水区”的组合：

1. 快速见效：权限感知的知识 RAG 或工艺变更 Copilot；
2. 战略深水区：良率异常根因、预测性维护、虚拟量测或 EDA Agent。

### 1.2 半导体制造的最小业务框架

面试前至少理解下面这条高层链路：

```text
芯片设计与 EDA
→ 晶圆制造（批次经过多道工序、设备、腔体和 recipe）
→ 晶圆测试
→ 封装与最终测试
→ 质量、出货与客户反馈
```

制造场景中的常见对象和数据：

- `lot / wafer`：生产批次和晶圆，是追踪生产履历的重要对象；
- `process step / tool / chamber / recipe`：工序、设备、腔体和工艺参数；
- MES：追踪批次、工序、设备和生产履历；
- FDC/设备数据：传感器、告警和运行趋势，用于异常检测和设备诊断；
- SPC、metrology、inspection：监控制程漂移、量测结果和缺陷；
- wafer map、test bin、yield：测试分布与良率结果，用于定位空间模式和异常影响。

推荐口述：

> 我目前掌握的是公开层面的制造框架，不会把自己包装成工艺专家。我的理解是，一个批次会经过多道工序和设备，最终质量问题可能与设备状态、recipe、量测、缺陷或测试结果有关。因此 AI 产品不能只做一个聊天入口，而要先确认具体角色在哪个决策点受阻、数据散落在哪些系统、真值和错误成本是什么，再选择时序、CV、规则、RAG 或 LLM 的组合。长鑫内部的系统名称、数据质量和责任边界，仍需要通过现场跟岗确认。

### 1.3 主 Case：工艺变更影响分析 Copilot

#### 2～3 分钟完整回答

> 我会先澄清四件事：用户是工艺工程师、审批人还是质量人员；当前变更从哪里发起、哪一步最耗时；有哪些数据和文档、版本及权限如何；AI 的结果只是辅助检查，还是会直接影响生产。
>
> 假设调研发现工程师需要跨多个系统查找历史材料，评审耗时且容易漏项，我会把 MVP 定义为“有证据的变更评审 Copilot”，不做自动批准。工程师在现有变更系统提交对象和原因后，系统在权限过滤的前提下检索最新版 SOP、历史变更、异常案例、风险项和测试要求，输出结构化卡片，包括可能影响的产品、工序、文档、测试、责任人、证据来源、尚缺信息和风险等级。工程师修改并确认后再写回原系统，全程保留审计记录。
>
> 技术上更适合 RAG 加受控 Workflow，而不是自由 Agent。文档入库需要补充版本、有效期、设备、产品、工序和 ACL；召回可以组合 BM25、向量检索和 rerank；输出使用固定 Schema，并要求关键结论绑定来源。必填项、版本、阈值和流程依赖由规则引擎校验，LLM 只做非结构化理解、归纳和候选建议。任何写入和审批动作都需要二次确认。
>
> MVP 先限定一个工序或一类变更，使用历史已审批案例构建回放集，再让资深工程师参与 shadow test。评估分三层：检索看关键文档 Recall@K、版本和权限正确率；回答看引用支持率、关键风险项覆盖、无依据结论和专家修改率；业务看评审周期、一次通过率、漏项、返工和使用率。严重漏项必须作为单独红线，不能被平均分掩盖。
>
> 上线顺序是历史回放、shadow recommendation、白名单建议模式、逐步扩大范围。任何低置信、证据冲突或高风险情况都回到人工处理，不让模型直接控制生产。

### 1.4 高频压力追问

**为什么用 RAG，不直接微调？**

> SOP 和变更资料频繁更新，且必须知道具体版本和来源，RAG 更适合事实注入与引用；微调可以用于稳定分类或表达风格，但不应被用来记忆最新版制度。

**如何证明 ROI？**

> 先测当前每单检索与评审时间、月度频次、返工和漏项基线。收益包括节省工程师工时和减少可归因的返工或风险，成本包括模型调用、数据治理、系统集成和人工复核。MVP 阶段不提前承诺良率提升。

**没有标注数据怎么办？**

> 用历史已审批变更及其引用材料构建回放集，由专家标注关键证据和风险项；上线后先 shadow，不影响生产，再持续收集采纳、修改和漏项。

**如何处理文档权限？**

> ACL 必须在召回前过滤，并在生成后再次校验来源权限；不能先召回机密内容，再只在 UI 层隐藏。

**文档冲突怎么办？**

> 优先有效版本和权威来源，并显式展示冲突文档与版本，不让模型自行拼成一个确定结论。

**用户不愿意用怎么办？**

> 将能力嵌入原有变更入口，自动预填减少额外录入；用来源和边界提高信任；与业务 champion 共创；根据 eligible-user 激活率、首次成功、重复使用和放弃原因迭代，而不是只靠行政推动。

### 1.5 其他可提及场景

#### 权限感知技术知识 RAG

- 数据：SOP、设备手册、故障案例、EDA 文档、测试规范；
- 产品要求：引用、版本、权限、拒答、反馈；
- 指标：检索耗时、首次解决率、引用正确率、采纳率、培训周期。

#### 良率与异常根因 Copilot

- 数据：lot 路径、设备时序、inline metrology、缺陷图像、wafer map、历史处置；
- 技术：CV、时序/异常检测负责识别，LLM 负责跨系统检索、解释和工作流编排；
- 指标：RCA 时间、误报漏报、恢复时间、工程师工时和良率损失暴露；
- 边界：第一阶段只提供候选根因和证据，不自动改 recipe。

若被问“具体解决什么问题”，可以直接回答：

> 我会聚焦异常批次的候选根因定位。痛点不是工程师完全没有数据，而是批次路径、设备趋势、量测、缺陷和测试结果分散在不同系统，历史处置经验也难复用。产品先把跨系统证据按时间和批次关联，再由专业模型识别异常信号，RAG 召回相似案例，大模型生成带证据的排查顺序。首期目标是缩短首次有效假设和 RCA 时间，而不是让大模型直接宣布唯一根因或自动调整工艺。

#### 预测性维护与虚拟量测

- 核心通常是时序模型和传统 ML，不是纯 LLM；
- LLM 可以承担自然语言交互、解释、工单总结和流程编排；
- 指标：非计划停机、MTBF、维护成本、过程偏移提前量。

#### EDA Agent

- 基于规格、设计规则、历史报告和工具日志辅助回归选择、debug 和根因分析；
- Agent 可以调用受控工具，但最终 sign-off 必须由工程师完成；
- 重点保护设计 IP、工具权限和审计链路。

#### 供应链、销售和财务

- 需求预测、WIP 排程、供应商风险、合同审阅、财务差异解释和销售知识助手；
- 预测与排程的核心往往是运筹优化、规则或传统 ML；
- LLM 更适合作为自然语言交互和工作流编排层。

---

## 二、AI 技术快问

### 2.1 大语言模型基本原理与能力边界

> 大语言模型通常基于 Transformer，通过自注意力建模上下文，预训练阶段学习预测下一个 Token，再经过指令微调和偏好对齐增强任务遵循能力。它擅长理解和生成非结构化内容、归纳和模式迁移，但输出本质是概率生成，不天然保证事实、逻辑、数字、时效和权限正确，也不等同于可靠的因果推理。
>
> 因此产品上不能只依赖 Prompt：事实交给 RAG 或工具，格式交给 Schema，确定性约束交给代码，高风险决策交给人工，并通过离线评测和线上监控持续验证。

补充：`temperature=0` 只能降低随机性，不能消灭幻觉。

若追问机器学习、深度学习和大模型的关系：

- 机器学习是让模型从数据中学习规律的总体方法，包括树模型、线性模型等；
- 深度学习是机器学习的一个子集，使用多层神经网络学习复杂表示，常用于图像、语音、时序和自然语言；
- 大语言模型通常是基于 Transformer 的大规模深度学习模型，重点处理语言理解、生成和工具交互；
- 产品选型不能把所有问题都交给 LLM：表格预测可能更适合树模型，设备时序适合异常检测或时序模型，缺陷图像适合 CV，大模型更适合非结构化理解、知识检索后的归纳和流程编排。

### 2.2 RAG 是什么，企业 RAG 怎么做？

> RAG 是先检索外部知识，再把相关证据作为上下文生成答案。完整链路不只是 Embedding 加向量库，还包括文档解析与切分、元数据、版本、ACL、混合召回、重排、Token Budget、引用校验、拒答和反馈。
>
> 设备号、错误码和文档编号更适合 BM25 或关键词检索，语义改写更适合向量检索，因此企业知识库通常适合 Hybrid Retrieval，再通过 RRF 或 rerank 合并结果。评估要把检索和生成分开：检索看 Recall@K、MRR/NDCG、版本和权限正确率；生成看事实准确、引用支持率、无依据结论和拒答正确率。

兴趣教练项目中可说：

> 我实现过 BM25、向量和 RRF 的混合召回，也做了时间和重要性重排；但当前没有足够大的人工标注召回集，因此不会把现有权重说成生产最优。

#### RAG 解决了什么，又没有解决什么？

RAG 主要解决：

- 模型预训练知识过期，无法及时获得最新制度和业务资料；
- 私有知识没有进入模型参数；
- 需要给结论绑定原始来源，便于核验和审计；
- 业务知识更新时，希望更新知识库而不是重新训练整个模型。

RAG 没有自动解决：

- 检索漏召回、召回错误或版本过期；
- 原始文档本身错误、冲突或没有权威真值；
- 模型拿到正确证据后仍然曲解或生成无依据结论；
- 权限隔离、数值计算、因果判断和高风险动作审批；
- 幻觉本身，只是为回答提供了更可靠的事实条件。

30 秒口述：

> RAG 解决的是“模型不知道最新或私有事实，也无法给出可核验来源”的问题，但它不等于消灭幻觉。检索可能漏掉正确文档、召回过期版本，模型也可能没有忠实使用证据。因此企业 RAG 必须把检索、生成和权限分开评估，关键结论绑定来源，低置信时拒答，高风险结果保留人工确认。

### 2.3 Agent、Workflow 和 Multi-Agent 的区别

> Workflow 的步骤由代码预先确定，最容易测试和审计；Agent 由模型根据状态动态选择下一步或工具；Multi-Agent 进一步把规划、评审等职责隔离，但会增加 Token、延迟和调试复杂度。
>
> 选择顺序应从确定性最高的方案开始：能用规则就不用 LLM，步骤固定就用 Workflow，只有工具和路径确实需要动态选择时才使用 Agent；Multi-Agent 还需要证明单 Agent 的失败来自职责冲突。

工业场景中的受限 Agent 需要：

- 工具白名单和最小权限；
- Schema 与参数校验；
- 最大轮次、超时和成本预算；
- 幂等、审计和失败降级；
- 高风险写操作人工确认。

个人项目证据：

> 兴趣教练中 Planner 和 Critic 负责生成和评审，Host 最终改成确定性 TypeScript 状态机，让模型处理开放语义、代码负责流程控制。简单任务仍然走单 Agent 或固定 Workflow。

### 2.4 Function Calling 是什么？

> Function Calling 不是模型直接执行函数，而是模型生成工具名和结构化参数，由宿主程序完成权限、Schema 和业务规则校验，再调用真实工具，并把结果返回给模型继续处理。

### 2.5 如何降低幻觉？

六层回答：

1. 输入：只提供相关、最新且有权限的证据；
2. 输出：使用 Schema、枚举和必填字段；
3. 证据：关键结论绑定 `source_id`，无证据明确拒答；
4. 工具：参数校验，并通过规则或数据库二次核实；
5. 流程：超时、最大轮次、降级、版本和审计；
6. 结果：golden set、hard negative、人工审核和线上抽检。

总结：

> Prompt 只能降低幻觉概率，不能提供确定性保证。风险越高，越要把动作权限从模型手里拿出来。

### 2.6 如何评估 RAG 或 Agent 产品？

不要只报一个总分，按失败链路分层：

| 层级 | 指标 |
| --- | --- |
| RAG 检索 | Recall@K、MRR/NDCG、版本正确率、权限正确率 |
| 生成质量 | 事实准确、引用支持率、无依据结论、拒答正确率 |
| Agent | 任务完成率、工具选择/参数正确率、平均轮次、失败恢复、越权率 |
| 工程 | P95、Token、超时、错误率、单次有效任务成本 |
| 线上产品 | 采纳、修改、任务完成、人工升级、节省工时、真实业务周期 |

评测集需要覆盖：

- 正常样本；
- 边界条件；
- hard negative；
- 高风险和越权样本；
- 历史真实失败案例。

LLM-as-judge 可以辅助规模化评测，但关键结果仍需专家校准和人工抽检。

### 2.7 Prompt、RAG 和微调怎么选？

- Prompt/Few-shot：任务变化快、主要是指令或输出格式问题；
- RAG：依赖私有、频繁更新的知识，并要求引用来源；
- 微调：数据足够且需要稳定的分类、风格或行为模式；
- 规则：日期、阈值、必填项、权限等确定性判断。

常见组合：

> Prompt 定义任务，RAG 提供事实，微调改善稳定行为，规则守住确定性边界。

不要用微调记忆频繁变化的 SOP，也不要为了使用大模型而替代已有确定性系统。

### 2.8 如何判断一个需求不应该使用 AI？

- 确定性规则已经能够稳定覆盖；
- 错误不可接受，同时没有人工或自动校验；
- 数据没有权限、质量不足或无法建立真值；
- 收益小于延迟、成本和维护复杂度；
- 用户问题本质上是流程未梳理或数据尚未数字化。

### 2.9 如何与算法团队有效沟通？

> AI 产品经理不应该只给算法团队一句“做一个 Agent”，也不应该越过算法同学直接指定模型结构。我会先把业务问题翻译成一个可评估任务：谁在什么流程中做什么决策，输入和输出是什么，有哪些数据与权限，当前基线是多少，成功指标和零容忍错误分别是什么，以及延迟、成本和部署限制。
>
> 再与算法、数据和研发一起确定技术基线、标注与评测集、离线指标和上线门槛。算法方案出现取舍时，不只比较模型分数，还要同时看错误分布、P95、成本、可解释性和人工复核负担。上线后把失败样本按数据、检索、模型、规则和交互分层归因，避免把所有问题都笼统归为“模型效果不好”。

可以联系自己的经历：

> Markdown 治理中，我先把模糊 badcase 分类并定义 Success、Fail、Worse，再决定哪些问题由确定性前端规则解决、哪些反馈给上游。这个方法可以迁移到与算法团队协作：先统一样本、口径和风险边界，再讨论模型和工程方案。

---
