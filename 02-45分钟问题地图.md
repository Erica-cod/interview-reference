# 45 分钟问题地图

## 可能的时间分配

| 时间 | 内容 | 我的目标 |
| --- | --- | --- |
| 0-3 分钟 | 自我介绍 | 主动抛出 ReactLynx、Markdown、监控三个钩子 |
| 3-16 分钟 | 实习/项目深挖 | 先讲难点和取舍，不堆技术名词 |
| 16-32 分钟 | JS、浏览器、网络、React | 每题 45-90 秒，结论先行 |
| 32-39 分钟 | 场景题或手写 | 先问边界，再给方案和复杂度 |
| 39-45 分钟 | 反问 | 问业务、技术难点和新人预期 |

## 本轮复盘后的训练优先级

| 优先级 | 必须修好的题 | 合格标准 |
| --- | --- | --- |
| P0 | ReactLynx 数据回设 | 90 秒内讲清一个组件/字段的首轮值、二次回写、改法和验证 |
| P0 | 监控 SDK + rrweb | 从一个 TypeError 讲清四个 ID、服务端聚合和回放 seek |
| P1 | 页面慢定位 | 用 TTFB/LCP/TBT/INP/CLS 把网络、JS、渲染瓶颈分开 |
| P1 | AI Agent 架构 | 讲清 Host 状态机、真实调度、混合记忆和数据库分层四个闭环 |
| P1 | RAF 流式渲染 | 先说每 chunk setState 的后果，再说 buffer + RAF 的机制和指标 |
| P2 | 企业业务系统 | 用一次状态流转讲接口契约、并发冲突、权限和测试 |
| P2 | 行为题 | 用 Markdown 治理说明如何主动定义模糊问题和评测口径 |

知识复习先服务于这些回答，不再继续无差别增加小抄数量。

## 第一优先级：近期禾赛前端高频

1. `==` 与 `===`，隐式转换有哪些坑？
2. JS 垃圾回收机制，什么情况容易内存泄漏？
3. 对象数组如何按 `id` 去重，复杂度是多少？
4. 同源策略和 CORS，什么时候发送预检？
5. 协商缓存和 `ETag` 如何工作？
6. 两次 `setCount(count + 1)` 为什么只加一，如何加二？
7. 什么是副作用，Effect 为什么需要 cleanup？
8. 简历中最有亮点的项目是什么？

## 第二优先级：JD 直接对应

1. 浏览器从输入 URL 到页面显示经历什么？
2. React 组件为什么重新渲染，如何减少无效渲染？
3. `useEffect`、`useLayoutEffect`、`useMemo`、`useCallback` 的边界。
4. UmiJS 解决什么问题？约定式路由与配置式路由怎么选？
5. Ant Design 大表格、复杂表单如何做性能和状态治理？
6. 首屏慢如何定位：网络、资源、JS 执行、渲染分别怎么看？
7. 研发/制造系统的权限、错误处理和可观测性怎么设计？

## 第三优先级：简历追问

### ReactLynx

- 什么是数据回设，如何通过 `__temp` / `UpdateComponentData` 定位？
- 拿一个组件和字段说：首轮值是什么，第二轮谁在什么时机写回什么？
- 如何用接口日志、资源加载记录和 trace 排除“只是接口慢”？
- 为什么懒读取、props 化、state 化能解决不同问题？
- 为什么不全部塞 state 或全部重构？
- 改完怎样验证？没有独立线上大盘数字时怎样准确表达？

### Markdown Repair

- 为什么不用正则直接扫原文？
- 前置和后置 Repair 如何选择？
- 如何避免 Repair Worse？
- 7.64% 到 3.23% 如何计算？

### AI Agent

- SSE 为什么不用 WebSocket？
- 流式 Markdown 半截结构怎么处理？
- 为什么用虚拟列表、RAF、BFF？
- 初版 Host 有什么问题，你是怎么发现并补齐闭环的？
- `next_agents` 如何真正控制下一轮？`must_address` 怎样传给下一个 Agent？
- 为什么相似度不能代表方案质量？现在真正的终止条件是什么？
- `revise / challenge / verify / finalize / terminate` 各在什么情况下触发？
- 为什么低共识不继续强制反方，而首轮高共识反而要压力测试？
- Host 趋势和断点恢复保存在哪里？达到最大轮次怎样保留未解决风险？
- 历史记忆为什么是“最近窗口 + BM25 + 向量 + RRF + 时间/重要性”？
- 为什么 `messages` 和 `memory_items` 分开？异步索引失败、消息更新或删除怎么办？
- 本地 fallback 与 Atlas Search 的边界是什么？QPS 上升 10 倍先优化哪些模块？
- 哪些能力是真正实现并测试的，哪些只是生产化改进方向？

### 监控 SDK

- 错误如何分类采集？
- SourceMap 为什么放服务端？
- rrweb 如何录制和回放？
- `eventId`、`fingerprint`、`sessionId`、`replayOffset` 分别何时生成、解决什么问题？
- 如何从一个 eventId 查询到错误前 10 秒、后 5 秒的回放分片并 seek？
- 采样、有限重试、队列溢出和隐私脱敏如何围绕这条链取舍？
- 如何保证 SDK 不影响业务？

## 通用回答框架

### 技术选型题

> 场景约束是什么 -> 候选方案有哪些 -> 为什么当前方案更匹配 -> 代价是什么 -> 如何兜底和验证。

### 项目难点题

> 现象 -> 定位证据 -> 根因 -> 方案对比 -> 我的动作 -> 指标 -> 如果重做。

### 不会的问题

> 这个细节我没有直接实践过。我先说我确定的边界……如果在项目里遇到，我会通过……验证，而不是直接假设。

## 技术方案取舍怎么答

不要从“某技术更先进”开始，而是按下面五步回答：

1. **场景约束**：数据量、实时性、端类型、团队规模、错误成本分别是什么？
2. **候选方案**：至少给出两个可行方案，先承认它们都能解决问题。
3. **核心权衡**：比较性能、复杂度、兼容性、可维护性、成本和上线风险。
4. **当前选择**：说明哪个约束权重最高，所以最终选了什么。
5. **兜底验证**：超时、降级、灰度、监控、回滚和指标怎么做。

### 可直接套用的话术

> 这个问题没有脱离场景的最优解。当时最重要的约束是……。我们比较了 A 和 B：A 的优势是……，但代价是……；B 在……方面更适合，不过会增加……。最终选 B，不是因为它绝对更好，而是它更匹配当前的……。为了控制它的风险，我们又加了……，并通过……指标验证。

### 高频取舍检查表

| 问题 | 优先确认 | 常见方案 |
| --- | --- | --- |
| 实时传输 | 单向/双向、消息频率、代理兼容 | SSE、WebSocket、轮询 |
| 大数据展示 | 是否连续浏览、行高是否固定 | 分页、虚拟列表、无限滚动 |
| React 状态 | 谁拥有、谁消费、是否需持久化 | local state、Context、外部 store、URL |
| 数据请求 | 密钥、编排、跨端复用 | 前端直连、BFF、统一网关 |
| 监控接入 | 定制深度、团队成本、合规 | 自研 SDK、Sentry/商业方案、混合方案 |
| AI 任务 | 是否确定性、是否可验证 | 规则、单模型、Workflow、Agent |
| 基建迁移 | 收益是否可测、回滚成本 | 原地优化、渐进迁移、一次性重构 |

## 如何应对连续追问

面试官通常会沿着以下链路追问：

```text
为什么选它
-> 为什么不选另一个
-> 这个方案最差的情况是什么
-> 数据量扩大十倍怎么办
-> 如何证明优化真的有效
-> 出问题如何回滚
```

准备项目时，每个亮点至少补齐这六个答案。没有真实指标时，可以说验证方案和观察指标，但不要编造收益。

如果面试官说“还是偏原则”，不要再换一种措辞复述原则，直接说：

> 好，我拿一个具体组件/字段（或一次请求、一条错误）的时间线说。

随后固定回答“改造前数据从哪来 -> trace/接口看到什么 -> 为什么形成风险 -> 实际改了什么 -> 改造后证据有什么不同”。

## 容易失分的表达

- 只说“性能更好”，没有指标、瓶颈和副作用。
- 把所有问题都归因于缓存、闭包或浏览器兼容。
- 把协助参与说成独立主导。
- 把离线 benchmark 说成线上生产系统。
- 答完概念后没有回到真实业务场景。
